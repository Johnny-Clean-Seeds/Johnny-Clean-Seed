# Sam Review Packet — Child Shell + Recursive Core Architecture

Generated: 2026-05-21T05:34:33Z

## Purpose

This packet packages the current proof stack into a Git-ready folder for public review.

It combines:

1. **Child Shell** — a proved local bridge pattern where an AI assistant requests named child actions instead of raw terminal access.
2. **Recursive Core Architecture** — the governing theory: clean placement of intelligence, controlled bridges, evidence before repair, mission lock, patch balance, and promotion through proof.
3. **LocalBot Machine Evidence** — earlier machine evidence showing a small local AI task loop with clear state, tests, recovery, failure capture, parked limits, and closeout.
4. **Visual doctrine boards** — supporting visual summaries for seed, mission lock, evidence before repair, patch balance, and recursive core growth.

## Main thesis

```text
The cure is not more intelligence.
The cure is clean placement of intelligence.
```

## Bridge proof formula

```text
AI request -> policy gate -> child action -> local function -> response + receipt
```

## What is proved now

The Child Shell V1 evidence run proved a bounded local bridge can:

- perform an allowed workspace write;
- block overwrite;
- block raw shell;
- block path traversal;
- block `.ps1` write;
- read/hash house files read-only;
- produce receipts/hashes;
- leave project state clean before and after.

## What this packet is not

This is not a claim of production-grade security.

This is not a claim that AI should receive broad computer control.

This is not a doctrine install.

It is a concept/formula/proof packet showing a small, bounded, auditable middle layer between chat-only assistance and broad computer-use agents.
