# Runtime Kernel Status Drop Source Ore

## Purpose

Capture the large architecture/status drop as source ore.

This is not a command to build.

The user explicitly said that milestone language and build language inside the drop are heads-up and food for thought, not commands.

## Source summary

The drop says the Clean Seed / Clean Milk brain has strong rules for seed behavior, proof, promotion, authority, parking, guides, and bridges, but needs a stronger runtime kernel.

The main pattern is:

- less loose guide text first
- more enforceable schema
- more ledger structure
- clearer state transitions
- stronger tests
- one recursive operating machine

## Captured pain points

1. Recursion is described but not yet enforced as one reusable machine.
2. Guides are heavy and may make current truth harder to keep sharp.
3. Hard Suit active pack slots are still empty.
4. There is no single system ledger that every part feeds into.
5. There is no universal state machine that every seed, checker, bridge, guide, and future bot follows.
6. There is no parent/child map that makes many moving parts behave as one moving part.
7. Seed contracts are described but not machine-enforced.
8. Runtime commands could help later but may be premature before contract/state clarity.

## Captured candidate fixes

1. Universal state machine
2. Single system ledger
3. Parent/child map
4. Seed schema / seed contract enforcement
5. Proof index
6. Decision ledger
7. Promotion ledger
8. Open-loop / blocked / parked maps
9. Hard Suit active-pack compression
10. Runtime command surface: cycle / check / status

## Active Soft Suit candidates

Only 3 are active now:

1. Universal state machine
2. Single system ledger
3. Parent/child map

Reason:

These three are the highest-load-bearing architecture helpers. They decide how later pieces move, report, and stay contained.

## Parked support candidates

These are captured but not active:

4. Seed schema / seed contract enforcement
5. Proof index
6. Decision ledger
7. Promotion ledger
8. Open-loop / blocked / parked maps
9. Hard Suit active-pack compression
10. Runtime command surface: cycle / check / status

Reason:

They are likely useful, but they depend on the first three being defined cleanly enough to avoid bloat and lane collision.

## Boundary

Do not implement a runtime kernel from this file.

Do not create /system yet from this file.

Do not install commands from this file.

Use this as source ore and boss-stack material only.

## Retrieval keys

- runtime kernel
- universal state machine
- system ledger
- parent child map
- seed contract
- proof index
- decision ledger
- promotion ledger
- open loops
- blocked items
- parked items
- hard suit compression
- cycle check status
