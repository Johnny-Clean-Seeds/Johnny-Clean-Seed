MR_KLEEN_LOCAL_BRIDGE_RULE_001
Status: ACTIVE SUPPORT NOTE / LOCAL ENTRY BRIDGE RULE
Authority: Not active doctrine.
Home: PUBLIC_NOTES
Date: 2026-05-17

PURPOSE

This note locks the Mr.Kleen local bridge rule.

The user has a dedicated Mr.Kleen shortcut/bridge that opens the current repo home.

Local PowerShell work should fit that bridge.

CORE RULE

When the user is already in the Mr.Kleen shell, do not force the full local path again.

Use the current shell as the room.

Verify the room instead of relocating unnecessarily.

CORRECT LOCAL ENTRY CHECK

For local PowerShell work, the assistant should first check:

- .git exists,
- origin remote matches Johnny-Clean-Seeds/Johnny-Clean-Seed,
- git status is clean or intentionally dirty for the named task,
- latest log matches expected current work.

HARD PATH FALLBACK

The known current local home is:

C:\Users\13527\Desktop\Jxhnny_Kleen_C3dz

Use this hardcoded path only as fallback when the shell is not already standing in the right room.

Do not keep forcing Set-Location to this path when Mr.Kleen already opened the room.

WHY THIS MATTERS

The Mr.Kleen shortcut is a clean local entry bridge.

Ignoring the bridge creates friction and increases transcript/paste confusion.

The assistant should use the tool that already fits the house.

LOCAL CODE RULE

When sending PowerShell for local use, label it:

THIS CODE IS FOR LOCAL

Keep local code separate from explanation.

Do not include terminal prompts, PASS/FAIL output, git output, or old transcript text inside the code block.

MODEL FIT

This is a tool-lane and command-lane support note.

It supports:

- source truth,
- paste safety,
- local command hygiene,
- assistant suit behavior,
- lock/save discipline,
- neighbor policy.

CLOSE RULE

This note must not outrank CURRENT_TRUTH_INDEX.txt.

This note must not outrank ACTIVE_GUIDES.

It supports local entry and should remain visible while useful.

When stale, archive or move it through the proper review gate.

Do not delete it.
