# Assistant Context Carrier Fit Report

Date: 2026-05-31
Status: FIT REPORT / SUPPORT ONLY / NOT DOCTRINE
WorkKey: ASSISTANT-CONTEXT-CARRIER-AND-ANCHOR-LEGEND-20260531-V1

## Source Inputs

The user asked to read:

`MULE_PACKET_HELPER_AUTONOMY_ROOT_CLEAR_SHAPE_GATE_JOB_20260531.md`

The root also contained:

`MULE_WORK_PACKET_ASSISTANT_CONTEXT_CARRIER_AND_VISIBILITY_GAP_20260531.md`

Both packets were read. The first packet is an older mule work order for shape-gate blocker classification, root read/apply/clear, and helper autonomy boundaries. The second packet is the direct fit source for this room: it names the failure class `CHAT CARRY CAN MASQUERADE AS HOUSE TRUTH` and asks for a compact, file-grounded context carrier with source maps and redaction.

## Finding

The house already has most of the pieces:

- Front Door / Bridge-Tunnel entry route;
- Recursive House Flow Spine;
- URL / LEDGER / MAP / KEY compression;
- helper capability candidate pointers;
- claim/capability bridge harness;
- proof-surface and lower-layer diagnosis rules;
- past-week idea/concept room;
- helper logic alignment room.

What was missing was one small carrier layer that says what outside/chat helpers may carry, what they must not assume, and when they need full files versus pointers.

## Decision

Create a support room and local-only carrier generator.

Tracked support:

- anchor legend;
- need-to-know load rules;
- chat assistant visibility card;
- existing-surface discovery;
- gap matrix;
- save/park decision;
- generator tool.

Local-only generated carrier:

- current context carrier;
- source map;
- does-not-prove note;
- redaction check;
- receipt.

## Owner Room

Owner room:

`HOUSE_WORK/SYSTEM_ALIGNMENT_ROOM/ASSISTANT_CONTEXT_CARRIER_20260531`

Why:

This object is mostly about agent/helper alignment, source maps, proof classes, and return contracts. Chat Cockpit can consume it later, but System Alignment is the clean owner for the shared rule.

## Need-To-Know Fit

Use full files only when the active task requires full structure:

- parser/shape/debug work on an exact target script;
- line evidence where context changes meaning;
- implementation review of a bounded target;
- redaction review of the carrier itself.

Use pointers or source-map rows when the active task needs orientation:

- current route;
- proof/receipt existence;
- status;
- Git state;
- root loose state;
- old source packets;
- long ledgers.

## Does Not Prove

This fit report does not prove the carrier is doctrine.
It does not make local generated carrier state permanently current.
It does not allow helpers to self-promote.
It does not let chat memory override file proof.
It does not replace receipts or final receiver review.
