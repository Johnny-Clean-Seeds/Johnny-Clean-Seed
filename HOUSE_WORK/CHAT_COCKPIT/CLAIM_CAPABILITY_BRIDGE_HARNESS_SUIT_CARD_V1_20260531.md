# Claim + Capability Bridge Harness Suit Card V1

Date: 2026-05-31
Status: CHAT COCKPIT SUPPORT CARD / NOT DOCTRINE
WorkKey: CLAIM-CAPABILITY-BRIDGE-HARNESS-20260531-V1

## Wear line

```text
Sentence -> claim split -> proof gap -> capability match -> helper legality -> return contract -> receiver assay -> disposition
```

## Quick blocks

- `helper passed` means split helper pass from job pass.
- `hash proves it` means custody only.
- `source says rule` means source ore only.
- `pattern triggered` means first-check only.
- `ready` means ready-for-what split.
- `make this a rule` means promotion request, not promotion.

## Helper rule

A helper can be dispatched only when the missing proof is clear, the input is exact, the capability is legal, and the return contract is known.

The helper cannot accept its own return. Receiver assay is separate.
