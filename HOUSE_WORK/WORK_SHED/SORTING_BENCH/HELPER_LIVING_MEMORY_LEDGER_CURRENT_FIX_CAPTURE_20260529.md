# Helper Living Memory Ledger — Current Fix Capture — 2026-05-29

Status: SORTING BENCH CAPTURE / CURRENT FIX DISSECTION  
Authority: support-only; not doctrine; not ACTIVE_GUIDES; not CURRENT_TRUTH_INDEX.  
Object: user's instruction that helper fixes must teach the files, not only the chat.

## Trigger

After the Freshen' up save route, the user corrected the shape:

The save must not merely preserve the phrase said in chat. The files need to understand the helper's old mistake, the new fix, and the full data needed to avoid repeating the issue.

## Immediate evidence

The Freshen' up save V1.1 proof showed a clean save and pushed helper save-content learning.

Current known final head from proof: `17fa83ae2007691d3a5f3903ff736f1927128d86`.

Saved issue-learning files from that run:

- `BRAIN_LEARNING/HELPER_SAVE_CONTENT_VALIDATION_AND_ISSUE_LEARNING_RULE_20260529.md`
- `HOUSE_WORK/WORK_SHED/SORTING_BENCH/FRESHEN_UP_SAVE_EMPTY_FIT_CONTENT_FAILURE_POWER_PLAY_20260529.md`
- `PROOF_HISTORY/FRESHEN_UP_RESET_ORIENTATION_RITUAL_RECEIPT_20260529_223932.txt`

## What was missing

The previous repair saved a rule and an issue card, but the user identified a larger architecture need:

Each helper should have a living ledger that accumulates helper-specific issue history and current truth.

The missing layer is not another generic rule. It is a mapped memory structure where each helper can carry:

- old behavior;
- old failures;
- false blame;
- actual root cause;
- fix mechanism;
- current behavior;
- proof receipts;
- rejected/superseded versions;
- next return triggers.

## New power play

When an issue is fixed, the fix should update the helper's living memory ledger before the work moves on.

This creates helper-specific continuity without forcing live chat to carry all raw history.

The ledger stacks useful data in the correct home. It does not reduce context by forgetting; it reduces live burden by mapping the context where it belongs.

## Fit judgment

FITS.

This matches existing house rules:

- helper-by-helper refinery;
- issue -> pause -> diagnose -> capture -> patch -> test -> continue;
- proof is in the pudding;
- Code Gate PASS is not job PASS;
- no broad panic audit;
- carry the key, not the furniture.

## Boundary

This save installs the living ledger idea and seed structure only.

It does not rewrite doctrine.
It does not rewrite ACTIVE_GUIDES.
It does not rewrite CURRENT_TRUTH_INDEX.
It does not create automation.
It does not refactor existing helper files.
It does not backfill every old helper ledger today.

## Next use

Next time a helper is touched, the helper-specific ledger should be updated or created as part of the fix closeout.

First likely targets:

1. Save Script Helper Family.
2. File Tools Main Menu Family.
3. Viewer Save Lifecycle Trial Family.
4. PowerShell Code Gate Runner Family.
