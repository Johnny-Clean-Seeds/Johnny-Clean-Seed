# Predictive Learning Mule Return Classification

Date: 2026-05-19
Base: main @ 2036d05
Lane: Work Shed / Sorting Bench
Input package: HOUSE_WORK\WORK_SHED\AGENT_OUTPUTS\PREDICTIVE_LEARNING_AND_MULE_FLOW_RETURN_20260519_001
Status: classification only, not doctrine

## Mule executive verdict

The mule's strongest correction is accepted:

Use the name:

Prior-Mistake Retrieval And Route Selection Gate

Do not use as active name:

Future-Prediction Learning Loop

Reason:
"Future prediction" can invite confidence theater. The real job is retrieval plus route selection.

## Adopt now

1. Name correction
   Adopt "Prior-Mistake Retrieval And Route Selection Gate" as the working candidate name.

2. Center correction
   The center is not mystical prediction.
   The center is:
   - classify current situation,
   - retrieve similar prior failures/wins,
   - choose route,
   - prove the chosen route advantage,
   - save only useful retrieval tags.

## Test as Soft Suit

Create a small Soft Suit / Work Shed card for:

Prior-Mistake Retrieval And Route Selection Gate

Candidate loop:
TRIGGER -> STATE CLASSIFY -> RETRIEVE SIMILAR CASES -> CHOOSE ROUTE -> PROVE ADVANTAGE -> ACT/PARK/BLOCK -> SAVE LESSON TAGS ONLY IF USEFUL.

Trigger:
- repeated failure,
- connected batch,
- old/new/stale mix,
- mule return,
- proof/receipt mismatch,
- long painful route starting,
- assistant awareness should have fired.

Route choices:
- batch safe-bin,
- one-by-one inspection,
- direct action,
- mule critique,
- park,
- block.

Proof fields:
- advantage type,
- evidence,
- tradeoff,
- risk,
- close condition.

## Adopt as companion candidate

Lesson tag template.

Minimum fields:
- lesson ID,
- failure mode,
- trigger phrase / condition,
- lane,
- artifact path,
- authority state,
- proof anchor,
- current-status risk,
- route chosen,
- route result,
- future-use trigger,
- do-not-repeat warning.

Use tags only when they improve future retrieval. Do not tag every note equally.

## Park / source-ore

Park for later:
- formal lesson index,
- big issue lab folder model,
- manifest checker,
- future retrieval helper,
- knowledge graph / graph database,
- dashboard,
- full case-based reasoning engine,
- formal ontology.

Reason:
Useful, but too heavy before the Soft Suit card proves value.

## Reject as active now

Reject now:
- full knowledge graph build,
- full runtime automation,
- numeric trust score pretending precision,
- blockchain / transparency-log custody,
- full FMEA ritual for every issue,
- automatic doctrine update from lesson index,
- auto-running tool candidates,
- mule output treated as steering authority,
- active guide rewrite.

## Tradeoff note

Batch safe-bin is better for:
- connected mule returns,
- checker batches,
- multi-file reports,
- companion artifacts.

One-by-one inspection is better for:
- executable scripts,
- private/security material,
- unclear provenance,
- authority-changing files,
- destructive or irreversible action risk.

## Verdict

PASS AS CLASSIFICATION.

Do not install doctrine yet.

Next move:
Build one Soft Suit card for Prior-Mistake Retrieval And Route Selection Gate and one lightweight tag template candidate.
