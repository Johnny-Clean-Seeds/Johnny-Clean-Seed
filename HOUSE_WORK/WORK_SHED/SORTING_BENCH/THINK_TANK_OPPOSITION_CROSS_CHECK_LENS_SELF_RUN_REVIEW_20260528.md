# Think Tank + Opposition Cross-Check Lens — Self-Run Review V1

Date: 2026-05-28  
Status: SELF-RUN REVIEW / PRE-SAVE PROOF-SUPPORT  
Object reviewed: THINK_TANK_OPPOSITION_CROSS_CHECK_LENS_RULE_CANDIDATE_V1.md  
Authority: not doctrine, not ACTIVE_GUIDES, not CURRENT_TRUTH_INDEX  
Boundary: rule reviews itself before save; no doctrine promotion  

---

## 1. Self-Run Verdict

```text
ACCEPT WITH GUARDRAILS / READY FOR SAVE ROUTE
```

The rule passes its own method.

It wore the relevant Think Tank pressure, checked neighboring house rules, used outside/opposite ideas as contrast, inverted rejected patterns into Clean Seed guardrails, and returned to the smallest next move.

---

## 2. Internal Think Tank Pressure

The saved Think Tank says the main goal is to reduce consumption burden and make the house carry orientation, proof, rules, maps, and next-action guidance.

Pressure applied to this rule:

```text
This rule must reduce future thinking burden.
It must not become another giant thinking ceremony.
It must help future rules build cleaner paths.
```

Think Tank also says:

```text
helpers stay workers;
path before door;
warnings are path signs;
dead chat becomes extracted key + proof trail;
fresh-reader proof matters.
```

Fit:

```text
This rule keeps helpers, rules, and path files from being judged only by live-chat momentum.
```

---

## 3. Neighbor Rule Pressure

### Neighbor 1 — Task Size Before Work

Pressure:

```text
size first, surface second, work third, proof closes.
```

Fit:

```text
The rule includes a scale rule so tiny syntax repairs do not trigger broad research.
```

### Neighbor 2 — Multi-Rule Batch Compatibility Apply Gate

Pressure:

```text
several rules at once means apply gate; run rules against each other before commit.
```

Fit:

```text
This rule strengthens that behavior by requiring self-run first and house-run when needed.
```

### Neighbor 3 — Patch-Through Before Fix Claim

Pressure:

```text
no list-to-guess; patch the fix through the gates, then judge from pressure map.
```

Fit:

```text
This rule requires the proposed rule to run through itself and neighbors before save.
```

### Neighbor 4 — No Parking Without Fit Decision

Pressure:

```text
no floating project gold.
```

Fit:

```text
This rule is not parked loose; it has a fit decision and save route.
```

### Neighbor 5 — Wayfinding Compatibility Review

Pressure:

```text
do not create new path files unless they answer a reader question and reduce burden.
```

Fit:

```text
This rule answers a reader/assistant question: how do we avoid shallow rule creation and opposite-blind designs?
```

---

## 4. Outside/Opposite Idea Checked

### Red Teaming

Useful outside idea:

```text
challenge assumptions and test a plan against adversarial or contrary views before failure.
```

House translation:

```text
Do not wait for a helper/workflow/rule to fail before asking how it could fail.
```

### Inversion

Useful outside idea:

```text
ask how the thing fails, then avoid the failure path.
```

House translation:

```text
Look at opposite-system designs to identify what Clean Seed should block or invert.
```

### Steelman

Useful outside idea:

```text
understand the strongest version of an opposing view before rejecting or adapting it.
```

House translation:

```text
Do not strawman outside systems. Find the useful pressure, then decide what to adopt, adapt, block, or invert.
```

---

## 5. Rejected Opposite Rules

Opposite rule rejected:

```text
Rule by momentum:
if an idea sounds good in chat, save it.
```

Clean Seed inversion:

```text
Rule must self-run before save.
```

Opposite rule rejected:

```text
Research drift:
look at every outside concept for every small task.
```

Clean Seed inversion:

```text
Use outside/opposite research proportionally for architecture, rules, gates, helpers, registries, workflows, and repeated failure branches.
```

Opposite rule rejected:

```text
Copy alien systems:
if another system has a mature method, import it whole.
```

Clean Seed inversion:

```text
Outside systems are contrast material, not authority.
```

Opposite rule rejected:

```text
Internal-only tunnel:
ignore outside and opposite patterns because the house already has rules.
```

Clean Seed inversion:

```text
Use outside/opposite concepts to expose hidden assumptions and generate cleaner guardrails.
```

---

## 6. Smallest Next Move

Smallest next move:

```text
Save the rule card and this self-run review as candidate/proof-support.
```

Do not:

```text
rewrite ACTIVE_GUIDES
rewrite CURRENT_TRUTH_INDEX
promote to doctrine
expand into a full new gate system
create a helper
resume entry-path patch before saving this rule
```

---

## 7. Proof Needed

Proof for this step:

```text
source hash of rule card and self-run review;
move/copy into correct house lanes;
destination hash compare;
receipt;
commit;
push;
final status clean;
boundary held.
```

Future proof:

```text
Use the rule on the next rule/gate/helper/workflow change and check whether it prevents shallow save or research drift.
```

---

## 8. What Not To Do

```text
do not treat this as doctrine;
do not use outside concepts as higher authority than house proof;
do not turn every tiny bug into a research project;
do not skip self-run for future rules;
do not save only in chat memory;
do not continue the stale entry-path patch until this rule is saved.
```

---

## 9. House-Run Need

House-run is needed because this rule affects:

```text
rule creation
save routes
helper behavior
registries
workflow design
repeated failure branches
assistant-facing behavior
```

House-run scope is bounded:

```text
neighbor rules checked;
no broad whole-house rewrite;
no active guide edits;
no current truth edits.
```

---

## 10. Self-Run Close

```text
SELF-RUN PASS WITH GUARDRAILS
READY FOR HASHED SAVE ROUTE
```

One-line close:

```text
The rule survives its own test: it uses Think Tank pressure, opposite-system contrast, and proportional house-fit checks without becoming research bloat or doctrine.
```
