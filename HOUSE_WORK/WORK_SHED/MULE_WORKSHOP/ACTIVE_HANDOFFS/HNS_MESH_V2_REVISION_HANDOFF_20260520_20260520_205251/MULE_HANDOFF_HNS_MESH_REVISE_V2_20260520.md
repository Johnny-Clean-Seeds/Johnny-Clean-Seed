# Mule Handoff - House Nervous System / Alarm-Debugger Mesh Version 2 Revision

Date: 2026-05-20  
Read First: YES  
Handoff Type: Mule revision / source-pattern cleanup / not authority  
Output Required: revised HNS Mesh packet materials, not doctrine  
Boundary: Mule supports Mr.Kleen. Mule does not command Mr.Kleen.

---

## 1. Mission

Revise the House Nervous System / Alarm-Debugger Mesh packet into a cleaner Version 2 using the house process.

Do not install doctrine.

Do not build runtime.

Do not build dashboard.

Do not rewrite ACTIVE_GUIDES.

Do not rewrite CURRENT_TRUTH_INDEX.

Do not treat outside research as house authority.

Do not treat mule output as command.

---

## 2. Read Order

The mule should read in this order:

1. This handoff file.
2. The prior House Nervous System / Alarm-Debugger Mesh research packet.
3. The corrected repair statement.
4. Any source files placed beside this handoff.
5. Any current active anchor/status file explicitly included by the user/assistant.
6. Then produce outputs into the named return lane.

If files are missing, state what is missing instead of inventing proof.

---

## 3. Required Inputs

Expected source material:

- `HOUSE_NERVOUS_SYSTEM_ALARM_DEBUGGER_MESH_RESEARCH_PACKET_20260520.md`
- `CORRECTED_HNS_MESH_REPAIR_STATEMENT_20260520.md`
- any chat notes or captured user corrections included with the handoff;
- any provided current house state/status/anchor excerpts;
- any included source index or research notes.

If these are not present, the mule must report the gap.

---

## 4. Required Outputs

Return a folder with these files:

### 4.1 `MULE_REVISED_HNS_MESH_V2_SOURCE_PATTERN.md`

A cleaner source-pattern candidate.

Must include:

- definition;
- boundary;
- core law;
- architecture map;
- sensor/alarm/debugger/router/reflex/memory/decay structure;
- alarm levels;
- sensor families;
- dead nerve detector;
- alarm storm detector;
- link to Valence Torus;
- link to mule custody;
- link to desktop delivery hygiene;
- link to proof systems;
- No Alarm King;
- tiny live-test rule.

### 4.2 `RAW_CAPTURE_AND_GROUPING.md`

Show raw captured ideas and group them.

Groups:

- sensors;
- thresholds;
- alarms;
- debuggers;
- routes;
- reflexes;
- memory/decay;
- hubs;
- link map;
- mule workflow;
- blocked moves;
- source support.

### 4.3 `REVISION_DELTA.md`

Show what changed from the prior packet.

Must include:

- what was strengthened;
- what was cut;
- what was merged;
- what was renamed;
- what stayed parked;
- what remains unproven;
- what should not be saved yet.

### 4.4 `REJECTION_LEDGER.md`

List rejected/blocked moves.

Must include:

- no doctrine;
- no runtime;
- no automation;
- no dashboard;
- no ACTIVE_GUIDES;
- no CURRENT_TRUTH_INDEX;
- no permanent alarm feed;
- no all-to-all graph;
- no mule-as-authority;
- no all-signals-every-task ritual.

### 4.5 `LINK_MAP.md`

Map relationships.

At minimum include:

- HNS Mesh -> Valence Council / Gated Valence Torus;
- HNS Mesh -> Desktop helper cleanup rule;
- HNS Mesh -> proof sensors;
- HNS Mesh -> mule custody;
- HNS Mesh -> source ore boundary;
- HNS Mesh -> Rule-Firing Confirmation;
- HNS Mesh -> MTER;
- HNS Mesh -> TODO/Boss handling;
- HNS Mesh -> No Rule King / No Alarm King;
- HNS Mesh -> delivery hygiene;
- HNS Mesh -> word-key clarity;
- HNS Mesh -> stale checks;
- HNS Mesh -> active ball/load gate.

For each link, classify:

- parent;
- child;
- sibling;
- neighbor;
- support;
- guard;
- conflict risk;
- future candidate.

### 4.6 `CLAIM_TO_SOURCE_MAP.md`

Map outside concepts to house claims.

Do not merely list links.

Each entry should include:

- outside source/concept;
- house concept supported;
- adaptation;
- boundary;
- what it does not prove.

### 4.7 `TINY_NERVE_TEST_TEMPLATE.md`

Create one minimal live-use template.

Must include:

- signal;
- lane;
- sensor;
- salience;
- threshold;
- alarm level;
- debugger;
- route;
- safe reflex;
- proof/boundary;
- memory/decay;
- close condition;
- No Alarm King check.

### 4.8 `MULE_WORKSHOP_CLEANUP_NOTE.md`

Explain how the mule handled current/old handoff materials.

Must say:

- what files were active;
- what files were old/stale if any;
- where old files were moved/should move;
- where outputs were returned;
- what remains open.

---

## 5. Mule Workshop Rule

The mule should have an active handoff room and storage/archive.

Suggested structure:

```text
MULE_WORKSHOP
-> ACTIVE_HANDOFFS
   Current handoff files to read/use now.

-> ACTIVE_DESK
   Live working material for current job.

-> RETURNS
   Completed outputs.

-> STORAGE_SHED or HANDOFF_ARCHIVE
   Old handoffs, superseded packets, stale source bundles.

-> INDEX
   Pointer to current active handoff and archive locator.
```

The mule has bounded freedom to shape this workshop.

He may improve the structure if his version is cleaner.

But he must keep current vs old separated.

He must not leave old handoffs mixed with current ones.

He must not treat old handoff material as current unless explicitly selected.

He must preserve custody.

---

## 6. PASS Standard

Mule output is PASS only if:

- all required input files were read or missing files were reported;
- raw capture exists;
- grouping exists;
- revision delta exists;
- rejection ledger exists;
- link map exists;
- claim-to-source map exists;
- final V2 source-pattern file exists;
- tiny nerve test template exists;
- mule workshop cleanup note exists;
- boundaries are stated;
- no doctrine/runtime/dashboard/automation install occurred;
- no mule-as-authority claim occurred.

---

## 7. Blocked Moves

Blocked:

- installing the HNS Mesh as doctrine;
- rewriting ACTIVE_GUIDES;
- rewriting CURRENT_TRUTH_INDEX;
- creating a runtime/automation/dashboard;
- treating outside research as proof of house authority;
- treating mule output as command;
- replacing house judgment with mule judgment;
- leaving old handoffs loose in active space;
- skipping source-to-claim map;
- skipping link map;
- skipping rejection ledger;
- skipping delta.

---

## 8. Return Lane

Return outputs to the named return folder supplied by the user/assistant.

If no return folder is supplied, create a clearly named return folder:

`RETURNS\HNS_MESH_V2_REVISION_RETURN_20260520`

Do not scatter outputs.

---

## 9. Completion Statement Required

The mule final note must state:

- what was read;
- what was produced;
- what was rejected;
- what remains unproven;
- whether the workshop was cleaned;
- where old handoffs went or should go;
- whether output is PASS / PARTIAL / BLOCKED.
