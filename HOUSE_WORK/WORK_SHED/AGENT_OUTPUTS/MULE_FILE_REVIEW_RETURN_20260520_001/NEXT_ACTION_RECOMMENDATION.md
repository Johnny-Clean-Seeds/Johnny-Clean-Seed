# NEXT ACTION RECOMMENDATION

## One Smallest Safe Install

ADAPT / USE IF APPROVED:

Create a tiny packet-scoped mule start card or add an equivalent top pointer block to the next active mule packet.

Recommended contents:

- active packet path,
- return folder path,
- branch/HEAD expectation,
- required local-state checks,
- exact authority-start files,
- exact task-scoped search rule files,
- exact mule-role and anti-agreement rule files,
- relevant proof receipts,
- blocked moves,
- finish condition.

Why this is the smallest safe install:

- It does not rewrite doctrine.
- It does not promote a tool.
- It does not create a full index, dashboard, lesson database, graph, or automation.
- It improves speed and safety by reducing search friction and stale-pointer risk at mule start.

Proof needed:

- A future bounded mule uses the card and correctly finds the active packet, governing rules, receipts, and return folder.
- The card does not conflict with `ACTIVE_ANCHOR.txt`.
- The card expires or is refreshed with the packet, so it does not become a stale boss.

## One Stronger Later Version

PARK:

If two or more later mule passes still show retrieval friction, create a controlled "bounded mule packet template" with a required top block and a small controlled tag row.

Possible tags:

- `ROLE:MULE`
- `SCOPE:BOUNDED`
- `AUTHORITY:RETURN_ONLY`
- `PROOF:HASH_REQUIRED`
- `RISK:STALE_POINTER`
- `RISK:FALSE_PASS`
- `MOVE:CRITIQUE`
- `MOVE:DISPOSITION`
- `MOVE:NO_INSTALL`

Why later, not now:

- The current flow is usable.
- One pass does not prove template overhead is worth it.
- A template is safer than a tool, but it is still a new house habit and should earn its keep.

Proof needed:

- Repeated mule packets miss fewer requirements after the template.
- Setup time drops without increasing authority mistakes.
- Tags improve retrieval without tag sprawl or false precision.

## Do-Not-Build-Yet List

BLOCK:

- Full lessons-learned database.
- Knowledge graph of house rules/proof/candidates.
- Automated mule router.
- Dashboard for current house state.
- Tool promotion for the Soft Suit or checker candidates.
- Active guide rewrite for mule or predictive-learning doctrine.
- Broad `BRAIN_LEARNING` reclassification pass.
- Proof-history restructure.
- Queue rewrite.
- Standing mule review cycle.

Why:

- These add maintenance drag and stale-pointer risk before proving a better finish condition.
- The house's current failure mode is not lack of a big system. It is point-of-work firing, receipt hygiene, and exact retrieval at handoff time.

## Proof Needed For Proposed Improvements

| Improvement | Proof Required |
|---|---|
| Packet-scoped mule start card | Future mule finds correct files faster and returns required files without authority drift. |
| Receipt supersession/disposition note | Intake artifact names which receipt is partial/superseded and which receipt proves packet placement. |
| Queue header wording cleanup | Narrow diff only; before/after hash; no work-item status drift. |
| Future packet top block | Future packet has fewer missed rules and less rereading. |
| Controlled tag row | Tags help retrieval in at least two real returns without multiplying categories. |
| Stronger packet template | Repeated mule pass evidence shows better speed or fewer misses with equal safety. |

## Immediate Next Move

EVIDENCE:

This return package is complete in the required output folder. No active doctrine, queue, anchor, guide, proof-history, or tool files were edited by this pass.

INFERENCE:

The next clean house-side move is intake/disposition of this return package. Do not run another mule by inertia.

Recommended next action:

1. Read `MANIFEST.md`.
2. Read `REVIEW_REPORT.md`.
3. Use `DISPOSITION_MATRIX.md` to accept, adapt, park, or block each item.
4. If the user authorizes a file edit later, choose only one small fix first: receipt supersession note or packet-scoped mule start card.

## Final Mule-Use Verdict

NO — house should apply, test, or disposition the current fixes first. Do not run another mule pass by default.

