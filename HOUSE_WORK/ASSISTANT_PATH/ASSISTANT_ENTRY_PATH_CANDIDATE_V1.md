# Assistant Entry Path — Candidate V1

Date: 2026-05-28  
Status: CANDIDATE / REVIEW FILE / NOT SAVE ROUTE  
Doc type: NAVIGATION / CURRENT STATE  
Authority: not doctrine, not ACTIVE_GUIDES, not CURRENT_TRUTH_INDEX  
Boundary: path file only; no helper code; no Git action; no doctrine promotion; no ACTIVE_GUIDES rewrite; no CURRENT_TRUTH_INDEX rewrite  

---

## 1. Target-Reader Scent

Target reader:

```text
fresh assistant / future worker / user returning after context loss
```

Target question:

```text
Where do I start right now without rereading the whole house or chewing old chat?
```

This file answers:

```text
main goal
current lane
live objects
parked objects
proven objects
not-PASS objects
best first files to read
next clean move
do-not-touch list
proof needed to close the next move
stale trigger
```

This file does not answer:

```text
full doctrine
full project history
all helper inventory
all proof history
all parked ideas
all local machine details
```

Best next file:

```text
Only read the named next file if this entry path cannot answer your immediate orientation question.
```

Proof pointer:

```text
This candidate proves itself only through the Fresh-Reader Orientation Test below.
```

Stale trigger:

```text
This file becomes stale when the current lane changes, a save route changes Git/head status, a parked branch resumes, helper-registry counts change, or Code Gate/local helper status changes.
```

---

## 2. Main Goal

```text
Reduce consumption burden by making the house self-orienting and proof-aware.
```

Plain form:

```text
Build a house that explains itself.
```

Operational form:

```text
Move the thinking burden out of live chat and into the house through paths, signs, proof pointers, custody, stable words, and clean next actions.
```

This is not a helper-building job. Helpers are workers. The path is the guide.

---

## 3. Current Lane

Current lane:

```text
HOUSE WAYFINDING + PROOF MEMORY SYSTEM / ENTRY PATH PROBE
```

Current mode:

```text
candidate path proof
```

Current object:

```text
ASSISTANT_ENTRY_PATH_CANDIDATE_V1.md
```

Current job:

```text
test whether one entry path can reduce orientation burden before creating a larger path system.
```

Current boundary:

```text
one candidate file only;
no seven-file set;
no helper registry repair yet;
no new helper code;
no save/push claim;
no doctrine promotion.
```

---

## 4. Live Objects

### Live object 1

Object:

```text
HOUSE_WAYFINDING_PROOF_MEMORY_SYSTEM_THINK_TANK_V1.md
```

Status:

```text
ACCEPT WITH GUARDRAILS / CANDIDATE SUIT / NOT PROMOTED
```

Use:

```text
wear as concept pressure:
path not door;
burden reduction;
proof before PASS;
helpers stay workers;
fresh-reader test;
dead chat becomes extracted key + proof trail.
```

Do not assume:

```text
not doctrine;
not ACTIVE_GUIDES;
not CURRENT_TRUTH_INDEX;
not ready to add as a set.
```

### Live object 2

Object:

```text
HOUSE_WAYFINDING_PROOF_MEMORY_SYSTEM_COMPATIBILITY_REVIEW_V1.md
```

Status:

```text
USEFUL REVIEW PACKET / COMPATIBILITY PRESSURE / NOT SAVE ROUTE BY ITSELF
```

Use:

```text
wear its gates:
Doc-Type Split;
Target-Reader Scent;
Fixed Custody Mini-Shape;
ADR Threshold;
Freshness / Drift;
Burden Budget;
Source-Required Compatibility.
```

Do not assume:

```text
not a command to create all files;
not a save route;
not proof that Tool Passport/Rainbow Road/hash-before-move were checked from exact sources.
```

### Live object 3

Object:

```text
ASSISTANT_ENTRY_PATH_CANDIDATE_V1.md
```

Status:

```text
CURRENT PROBE OBJECT
```

Use:

```text
judge whether one entry path can answer enough orientation questions to reduce live-chat burden.
```

---

## 5. Parked Objects

### Parked object 1

Object:

```text
CONTROLLED_VOCABULARY_V1.md
```

Status:

```text
PARK WITH RETURN TRIGGER
```

Return trigger:

```text
Split this into its own reference file only if the Fresh-Reader Orientation Test fails because status/path/proof words are unstable or confusing.
```

Reason parked:

```text
A standalone vocabulary file may become reference furniture before one path proves the need.
```

### Parked object 2

Object:

```text
HELPER_REGISTRY_REPAIR_PLAN_V1.md
```

Status:

```text
PARKED REAL PROOF BRANCH
```

Known pressure:

```text
helper files found: 263
registered helpers: 91
missing from registry: 172
unclassified duties: 23
verdict: NOT PASS
```

Return trigger:

```text
After entry path proves orientation value and vocabulary needs are clear.
```

### Parked object 3

Object:

```text
PROOF_INDEX_V1.md
```

Status:

```text
PARKED
```

Return trigger:

```text
After the entry path test shows proof lookup is the main missing answer.
```

### Parked object 4

Object:

```text
FOLDER_MANIFEST_CANDIDATES_V1.md
```

Status:

```text
PARKED
```

Return trigger:

```text
After repeated confusion proves which folders need signs at decision points.
```

### Parked object 5

Object:

```text
HOUSE_WAYFINDING_PROOF_MEMORY_SYSTEM_V1.md
```

Status:

```text
PARKED EXPLANATION FILE
```

Return trigger:

```text
After entry path proves the model reduces burden.
```

---

## 6. Proven Objects

### Proven object 1

Object:

```text
PowerShell Code Gate Runner V1.4 installed wrapper
```

Status:

```text
LOCAL HELPER INSTALLED_PROVEN / V1.3 FALLBACK PRESERVED
```

Proof summary:

```text
Installed V1.4 prints:
POWERSHELL CODE GATE RUNNER V1.4
Mode: installed wrapper / console warning-title upgrade
V1.4 INSTALLED WRAPPER SELF-CHECK COMPLETE
V1.4 INSTALLED WRAPPER RUN COMPLETE
```

Boundary:

```text
V1.3 owns parsing/run verdict.
V1.4 only adds warning-title console visibility.
Not doctrine.
Not Git.
Not ACTIVE_GUIDES.
Not CURRENT_TRUTH_INDEX.
```

### Proven object 2

Object:

```text
Exact lifecycle label repair for V1.4
```

Status:

```text
PASSED
```

Proof summary:

```text
Old marker remaining: False
New marker present: True
Candidate marker remaining: False
```

### Proven object 3

Object:

```text
Hash-before-move custody rule
```

Status:

```text
CHAT-BEHAVIOR RULE CAPTURED / LOCAL CUSTODY RULE
```

Working rule:

```text
Do not move project/custody files before hashing them.
For files: source hash -> move -> destination hash -> compare -> receipt.
For folders: pre-move manifest -> move package -> post-move manifest -> compare -> receipt.
```

Boundary:

```text
V1.4 is not a mover or hasher.
Hash helper should remain separate.
Move helper may later consume hash proof.
```

---

## 7. Not-PASS / Watch Objects

### Watch object 1

Object:

```text
Helper registry completeness
```

Status:

```text
NOT PASS / PARKED REAL PROOF BRANCH
```

Reason:

```text
Registry gap exists but should not be repaired before the entry path proves orientation value.
```

### Watch object 2

Object:

```text
Full path-system file set
```

Status:

```text
NOT READY / BLOCKED AS SET
```

Reason:

```text
Creating seven files at once risks a map pile before proof.
```

### Watch object 3

Object:

```text
Tool Passport / Rainbow Road / exact source compatibility
```

Status:

```text
NEEDS SOURCE CHECK BEFORE SAVE ROUTE
```

Reason:

```text
Compatibility cannot be claimed until exact source objects are read or provided.
```

---

## 8. Best First Files To Read

Read only as needed.

1. Current entry path candidate:

```text
ASSISTANT_ENTRY_PATH_CANDIDATE_V1.md
```

Purpose:

```text
start here; current lane and next move.
```

2. Compatibility review packet:

```text
HOUSE_WAYFINDING_PROOF_MEMORY_SYSTEM_COMPATIBILITY_REVIEW_V1.md
```

Purpose:

```text
explains why the path system must narrow before expansion.
```

3. Think Tank wash:

```text
HOUSE_WAYFINDING_PROOF_MEMORY_SYSTEM_THINK_TANK_V1.md
```

Purpose:

```text
source concept; wear as candidate suit, not doctrine.
```

4. Chat cockpit card:

```text
CHAT_DROP_COPY__CHAT_HOUSE_RULES_COCKPIT_CARD_APPLIED_20260522.md
```

Purpose:

```text
front-door task shape, no fake PASS, no broad movement, no save promotion.
```

Do not read the whole house unless this entry path fails.

---

## 9. Next Clean Move

Next move:

```text
Run Fresh-Reader Orientation Test V1 on this candidate file.
```

Do not create another path file first.

Do not create helper code first.

Do not repair helper registry first.

Do not save/push first.

The next move is judgment:

```text
Can this single file answer enough orientation questions to reduce burden?
```

---

## 10. Fresh-Reader Orientation Test V1

Using only this file, answer:

```text
1. What is the main goal?
2. What lane is live?
3. What is parked?
4. What is proven?
5. What is NOT PASS or on watch?
6. What helper/status map matters next?
7. What should not be touched?
8. What is the next clean move?
9. What proof would close that move?
10. What does PASS mean here?
```

Pass standard:

```text
8/10 answers cleanly from this file
no broad scan needed
no old chat needed
no helper code needed
no doctrine claim
```

Fail standard:

```text
reader needs old chat;
reader confuses PASS with job proof;
reader cannot find current lane;
reader cannot name the next move;
reader cannot tell parked from live;
reader needs broad scan to orient.
```

If fail:

```text
repair this entry path before creating any new path file.
```

If fail because words are unstable:

```text
split out CONTROLLED_VOCABULARY_V1.md.
```

If pass:

```text
decide the next smallest path object from the missing-answer pressure.
```

---

## 11. Do-Not-Touch / Do-Not-Assume List

Do not touch:

```text
ACTIVE_GUIDES
CURRENT_TRUTH_INDEX
doctrine files
Git commit/push route
helper registry repair
Tool Passport / Rainbow Road source claims
folder manifest creation
proof index creation
new helper code
move helpers
cleanup/move operations
```

Do not assume:

```text
candidate means accepted;
Code Gate PASS means job PASS;
a review packet means save route;
one useful concept means seven files;
a title means good scent;
source/custody can stay vague;
old chat should stay live once key/proof is extracted.
```

---

## 12. Words Used Here

This is a small vocabulary strip, not a full controlled vocabulary file.

MAIN GOAL:

```text
The active parent purpose this work must serve.
```

PATH:

```text
A readable orientation object: file, map, sign, receipt, proof pointer, next-action card, registry entry, or status marker.
```

DOOR:

```text
A new execution/control surface: service, protocol, crawler, helper swarm, automation surface, or broad moving system.
```

CANDIDATE:

```text
Useful object under review; not promoted.
```

PROOF:

```text
Evidence that the object did the job it claimed, under the right boundary.
```

PASS:

```text
Close only when required checks pass together and the object did its job.
```

PASS WITH WATCH:

```text
Usable but monitored because warnings, boundaries, partial risks, or future checks remain.
```

NOT PASS:

```text
The object did not yet prove its job.
```

PARKED:

```text
Useful but inactive, with reason and return trigger.
```

STALE:

```text
No longer safe to trust without review because a drift trigger fired.
```

BURDEN:

```text
Future reading, scanning, explaining, code generation, re-chewing, or confusion this path should reduce rather than add.
```

---

## 13. Burden Budget

This file removes:

```text
repeated explanation of the current wayfinding object;
broad scan pressure before knowing the next object;
confusion between live, parked, proven, and not-PASS branches;
pressure to create seven files immediately;
pressure to build helper code before judgment.
```

This file may add:

```text
one maintenance object that must be updated when the current lane changes.
```

Burden verdict:

```text
REDUCES BURDEN WITH MAINTENANCE COST
```

Maintenance rule:

```text
If this path is saved later, it needs a visible stale trigger and update/receipt path.
```

---

## 14. Fixed Custody Mini-Shape

Object:

```text
ASSISTANT_ENTRY_PATH_CANDIDATE_V1.md
```

Entity/source:

```text
current uploaded Think Tank and Compatibility Review packets; current chat proof state; cockpit behavior card; recent V1.4 proof state.
```

Activity/action:

```text
created as one-file path probe after house walk and compatibility narrowing.
```

Actor/tool:

```text
ChatGPT in current conversation; no local helper code.
```

Inputs:

```text
HOUSE_WAYFINDING_PROOF_MEMORY_SYSTEM_THINK_TANK_V1.md
HOUSE_WAYFINDING_PROOF_MEMORY_SYSTEM_COMPATIBILITY_REVIEW_V1.md
CHAT_DROP_COPY__CHAT_HOUSE_RULES_COCKPIT_CARD_APPLIED_20260522.md
recent pasted V1.4 proof outputs
```

Outputs:

```text
this candidate markdown file
```

Receipt/proof:

```text
pending Fresh-Reader Orientation Test V1
```

Hash/commit:

```text
not hashed here; not committed; not pushed
```

Trust state:

```text
candidate / review-only / not promoted
```

Current status:

```text
ready for fresh-reader orientation test
```

What must not be assumed:

```text
not doctrine;
not ACTIVE_GUIDES;
not CURRENT_TRUTH_INDEX;
not a save route;
not proof that the whole path system works.
```

Drift trigger:

```text
stale when current lane, proof state, parked branches, helper registry counts, or Code Gate/helper status changes.
```

---

## 15. Close Condition

This candidate closes only when one of these happens:

```text
PASS: Fresh-Reader Orientation Test answers at least 8/10 cleanly from this file.
REPAIR: Test exposes missing current-lane, proof, or next-action clarity.
SPLIT: Test proves vocabulary confusion requires CONTROLLED_VOCABULARY_V1.md.
PARK: User stops path work with return trigger.
BLOCK: File adds burden or causes false authority.
```

Current close status:

```text
PENDING FRESH-READER ORIENTATION TEST
```
