STAGE 2 CLOSEOUT - FAILURE HANDLING

Active target:
CleanMilkChecker

Stage closed:
Stage 2 - Failure Handling

Failure captured:
C:\CleanSeedsBuild\CleanMilkChecker\failures\STAGE_2_FAILURE_001_BAD_VARIABLE_CAPTURE.txt

What Stage 2 proved:
CleanMilkChecker can preserve and judge a real failure instead of pretending it passed.

Failure used:
PowerShell command used $Input as a normal variable name during TEST 005.

Expected result:
The command should print the full TEST 005 input path.

Actual result:
The command printed:
PASS wrote TEST 005 invalid input:

Why this mattered:
The command printed PASS but did not print the expected file path, so the result was INVALID.

What passed:
The failure was noticed.
The bad output was not treated as PASS.
The failure was captured before being buried.
The cause was named.
The repair target was named.
The repaired command used $InputFile and printed the correct full path.

What failed:
Nothing blocks Stage 2 close.

Dirty milk removed:
False PASS from incomplete command output.
Unsafe variable naming.
Trusting the word PASS without checking the printed evidence target.

Trusted use now:
CleanMilkChecker can capture visible failure evidence and preserve the repair lesson.

Not trusted yet:
Not trusted to recover automatically after interruption or stale state.
Not trusted to scan folders automatically.
Not trusted to run unattended.

Final Stage 2 verdict:
PASS

Next clean state:
Open Stage 3 - Recovery. Prove that interrupted, stale, missing, or incomplete work can be recovered or safely failed with evidence.
