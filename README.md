# Johnny Clean Seed — Repo Home Front Door

This repo home is the assistant/model brain for this build.

It may be publicly reachable, but public reach does not define its purpose or authority.

The purpose of this home is clean assistant entry, routing, memory, proof, and action.

## Start here

Read in this order:

1. `CURRENT_TRUTH_INDEX.txt`
2. `ACTIVE_GUIDES/README_START_HERE.txt`
3. `ACTIVE_GUIDES/CLEAN_SEED_WRAP_GUIDE.txt`
4. `ACTIVE_GUIDES/CLEAN_SEED_ALIGNMENT_CHECK.txt`
5. `AGENTS.md` only when doing local file-lane / Codex-style work

## Authority map

- `ACTIVE_GUIDES/` is the active guide packet.
- Root guide files are pointer/reference files unless `CURRENT_TRUTH_INDEX.txt` proves otherwise.
- `CURRENT_TRUTH_INDEX.txt` is the current orientation ledger.
- `AGENTS.md` is local file-lane guidance. It is not a fourth active guide.
- `PROOF_HISTORY/` is evidence, receipts, backups, reports, and audit material. It is not active doctrine by volume.
- `PUBLIC_NOTES/` is the support-note lane for visible signs, cards, handoffs, learning notes, and public-boundary notes. It is not active doctrine by default.
- `TEST_LANES/` is experimental/test scope only.
- `Shed/` is source-ore/reference material unless deliberately promoted through the model.

## Public/local boundary

`CURRENT_TRUTH_INDEX.txt` may mention private/local paths because it tracks the working house.

Publicly visible paths or notes are orientation facts unless the active authority chain says otherwise.

Public readers must not treat local paths as commands for their own machine.

## Home language rule

Use `home`, `house`, or `brain` when judging function and authority.

Use `public` only when describing exposure, access boundary, or a named folder such as `PUBLIC_NOTES`.

Do not judge this home by generic human-facing polish unless that polish improves assistant entry, routing, authority clarity, proof flow, memory, or word-key precision.

## Clean rule

Do not promote, delete, move, overwrite, or call PASS from this home view alone.

Use the active guides, current truth index, named evidence, and user approval path.

## Word-key support

Words are keys.

For wording notes and non-authority definitions, see:

`PUBLIC_NOTES/WORD_KEYS_AND_PUBLIC_LANGUAGE_NOTES_001.txt`

For the home/public coexistence rule, see:

`PUBLIC_NOTES/HOME_LANGUAGE_COEXISTENCE_RULE_001.txt`

Those notes help keep words clean without replacing the active guide packet.

## Why this file exists

This README removes entry ambiguity.

It does not rewrite the model.

It routes the assistant to the active packet and labels proof/history/source-ore correctly.
