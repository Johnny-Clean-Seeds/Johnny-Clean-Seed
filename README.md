# Johnny Clean Seed — Public Repo Front Door

This public GitHub repository is a public reference house for the Clean Seed project. It is not, by itself, the private local source of truth.

Start here in this order:

1. `CURRENT_TRUTH_INDEX.txt`
2. `ACTIVE_GUIDES/README_START_HERE.txt`
3. `ACTIVE_GUIDES/CLEAN_SEED_WRAP_GUIDE.txt`
4. `ACTIVE_GUIDES/CLEAN_SEED_ALIGNMENT_CHECK.txt`
5. `AGENTS.md` only when doing local file-lane / Codex-style work

Authority map:

- `ACTIVE_GUIDES/` is the active guide packet.
- Root guide copies are public mirror/reference copies unless `CURRENT_TRUTH_INDEX.txt` says otherwise.
- `CURRENT_TRUTH_INDEX.txt` is the current orientation ledger. It may mention private/local paths because it tracks the working house. Public readers must not treat private paths as public repo commands.
- `AGENTS.md` is local file-lane guidance. It is not a fourth active guide.
- `PROOF_HISTORY/` is evidence, receipts, backups, reports, and audit material. It is not active doctrine by volume.
- `PUBLIC_NOTES/` is public-facing notes, handoffs, test packets, and route material. It is not active doctrine by default.
- `TEST_LANES/` is experimental/test scope only.
- `Shed/` is source-ore/reference material unless deliberately promoted through the model.

Clean rule:

Do not promote, delete, move, overwrite, or call PASS from this public view alone. Use the active guides, current truth index, named evidence, and user approval path.

Public weak-link fix installed by this file:

This README exists to remove front-door ambiguity. It does not rewrite the model. It routes the reader to the active packet and labels proof/history/source-ore correctly.
