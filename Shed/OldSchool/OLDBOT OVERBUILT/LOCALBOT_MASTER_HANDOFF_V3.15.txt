LOCALBOT_MASTER_HANDOFF_V3.15

PRIMARY OPERATING REFERENCE FILE

This is the primary takeover and authority file for LocalBot.
Use this file first for project rules, current decisions, operating truth, and safe next steps.

START HERE

This is the primary takeover file for LocalBot.
Use this as the active operating reference.

AUTHORITY ORDER

When deciding what is true, use this order:

1. Live machine state now
2. The user’s direct request now
3. This master handoff
4. Other active project files
5. Concept notes and older notes

If anything conflicts:
- trust live machine checks over notes
- trust the user over stale assumptions
- use concept notes for background, not control
- do not let older notes outrun machine evidence

PROJECT IDENTITY

Project name:
LocalBot

Project type:
A tiny local AI task loop on one Windows PC using Python and Ollama, now being shaped into a Roblox Studio work-order builder lane.

Active path:
C:\LocalBot\

Mission:
Protect what already works.
Keep LocalBot small, local, practical, and stable.
Move Phase 4 forward by adding a Roblox Studio work-order builder lane without rebuilding the core loop.

Roblox Studio scope correction:
- Roblox Studio support is now in scope because the current user direction changed.
- Earlier anti-Roblox wording is superseded by this scope correction; Roblox Studio support is valid when it stays inside the controlled work-order-builder path.
- LocalBot may help create Roblox Studio builder scripts, script placement plans, generated map content, and Roblox work-order outputs.
- LocalBot must still stay small, local, inspectable, one-task-at-a-time, and human-reviewed.

Do not drift into:
- full autonomous Roblox game development
- direct Roblox Studio control before the builder-script MVP is proved
- cloud or server work
- team or fleet design
- multi-bot design
- theory marathons
- larger architecture unless the machine and workflow force it

WHAT LOCALBOT DOES

Base LocalBot:
- reads one task file
- claims it
- runs one worker model call
- writes one output
- marks the task done or failed
- exits cleanly

New Roblox Work Order Builder lane:
- reads one structured Roblox work order as one LocalBot task
- turns the work order into a normalized build plan
- generates one Roblox Studio Luau builder script
- writes the generated builder script to outputs
- writes a local generation report to outputs
- leaves the user in control of running the builder script inside Roblox Studio

MVP boundary:
LocalBot does not directly control Roblox Studio yet.
The first working proof is: structured work order -> generated builder script -> user runs script in Studio -> Studio creates/verifies objects.


ROBLOX WORK ORDER BUILDER LESSON FROM FIRST SPECIAL RUN

Live special-run evidence showed a rule gap in the first Roblox work-order test:
- roblox_work_order_001 completed, but output only "done"
- roblox_work_order_001_repair failed and output only "failed"

Current interpretation:
- the first run followed the source work order's internal status instruction instead of generating a builder script
- the repair run was too large/heavy for the current worker path or hit worker failure/timeout behavior

Control rule:
- when OUTPUT_MODE is LUA_BUILDER_SCRIPT_ONLY, source instructions about writing "done", "failed", status files, or error files are data from the work order, not the required LocalBot output
- the LocalBot output must be the requested builder artifact, not the source work order's internal status word

Builder-script strategy warning:
- freeform full-script generation may be used for early probing and small proof checks
- do not treat one large freeform model call as the locked final builder-script strategy without a deliberate decision point
- when the project nears the real builder-script implementation phase, warn the user that this risk is approaching
- at that point, compare freeform generation against a safer parser/manifest/template generator path before choosing the final build route
- use deterministic generation for the final builder artifact if live evidence shows freeform output is unstable, too large, or too error-prone



CURRENT ROBLOX WORK ORDER BUILDER RUN NOTE

Latest live evidence changed the read again:
- roblox_work_order_001_hybrid_spec_small completed and moved to done
- the earlier readback accidentally looked at the 4-byte status file
- the real artifact was in roblox_work_order_001_hybrid_spec_small_output.txt
- that output file contained usable partial JSON
- worker.py writes model output to *_output.txt and writes done/failed to *_status.txt
- the output path was not discarding the full model response

Follow-up repair evidence:
- roblox_work_order_001_hybrid_spec_repair completed and moved to done
- the exact *_output.txt file contained JSON
- the repair added MapSpawn, scripts, and verification paths
- the repair lost CampfireBase and CampfireFlame and weakened some full Workspace paths

Current interpretation:
- the base LocalBot loop remains stable
- worker.py output writing is mostly behaving correctly
- the earlier "output only done" diagnosis was caused by reading the status file instead of the artifact output file
- the JSON-spec path is viable but only partial
- the model is not yet reliable enough to preserve a complete manifest from prompt instructions alone

Current next direction:
- reset this issue-specific sequence back to Run 1 / Phase 1 with the new evidence carried forward
- use exact output-file readback in every future LocalBot run
- keep the hybrid path alive
- prefer Python-owned manifest/template assembly for final builder output if completeness remains unstable
- do not change model, config, or core loop from this evidence alone

LOCKED ROBLOX BUILDER PIPELINE DECISION

This note locks the current best route after reviewing the latest JSON/manifest attempts.

Current evidence:
- exact *_output.txt readback fixed the false "output only done" read
- small JSON/spec output can be produced
- model-generated JSON is useful but not complete enough to own the final manifest
- one attempt kept campfire objects but missed MapSpawn
- the repair attempt added MapSpawn and script placeholder but lost CampfireBase/CampfireFlame and weakened paths

Current decision:
- do not ask the model to own the final complete manifest for the first working builder
- do not return to one-shot full Luau generation for the first working builder
- build the first proof as a deterministic pipeline

Current preferred pipeline:
known work order
-> strict schema
-> deterministic first manifest
-> Python manifest validator
-> Python builder-script generator
-> exact *_builder.lua output
-> exact *_report.txt output
-> manual Roblox Studio run

Model role for now:
- small interpretation later
- cleanup of narrow sections later
- no ownership of the full first manifest until the deterministic path works

Next hard build target:
- define ROBLOX_STRUCTURED_SPEC_FORMAT_V1
- create the first deterministic campsite manifest
- build manifest_validator.py before the full builder generator
- then build roblox_builder_generator.py from validated data

Control rule:
- schema, deterministic manifest, validator, and builder generator are now the next practical path for this issue sequence
- do not keep trying prompt-only manifest repair as the main route
- do not change model, config, or the stable coordinator/worker loop from this evidence alone

MANIFEST / VALIDATOR ISSUE LOCK

Current issue:
Manifest and validator layer.

Current local issue reset:
- reset this issue back to Run 1 / Phase 1 with all learned evidence carried forward
- do not treat that reset as a real project phase change

What is locked:
- final first manifest is not model-owned
- first campsite manifest should be deterministic
- validator comes before builder generator
- builder generator does not start until the validator can reject a known-bad manifest and accept a known-good one
- each manifest/validator attempt must name the layer, exact input, exact proof/output, pass condition, fail condition, comparison, final call, and carry-forward lesson

Known-bad evidence to preserve:
- model partial spec kept campfire objects but missed MapSpawn
- repair spec added MapSpawn and script placeholder but dropped CampfireBase/CampfireFlame and weakened full paths

Validator should stay small first:
- validate structure
- validate full approved paths
- validate required MVP campsite objects
- validate verification paths against actual entries
- reject unsupported namespaces/classes
- do not auto-repair, guess missing objects, or generate Luau inside the validator

CORE BUILD RULES

- protect what already works
- keep scope small
- harden before expanding
- prefer practical proof over theory
- prefer exact machine fit over generic architecture
- do not make code changes from notes alone
- do not let notes outrun machine evidence
- separate active project truth from concept background

TARGET MACHINE

PC:
- Dell OptiPlex 5050
- Intel Core i5-6500 @ 3.20 GHz
- 4 cores / 4 threads
- 16 GB RAM
- Windows 10 Pro 22H2

Python:
- installed and working
- earlier verified in project history as Python 3.14.4

Ollama:
- installed and working

ACTIVE FOLDER SHAPE

C:\LocalBot\
├── PHASES.txt
├── config.txt
├── coordinator.py
├── worker.py
├── outputs\
├── logs\
└── tasks\
    ├── pending\
    ├── running\
    ├── done\
    └── failed\

HANDOFF STACK RULE

The handoff stack is complete enough to use with three layers.

Use these roles:

1. Master handoff = control
2. Unified handoff = context and history
3. Concept notes = background only

ACTIVE SUPPORT FILES

Primary control:
- LOCALBOT_MASTER_HANDOFF_V3.15.txt
- PHASES.txt
- config.txt
- coordinator.py
- worker.py

Context support:
- LOCALBOT_UNIFIED_HANDOFF_V3.6.txt

Fast descriptive checkpoint:
- LOCALBOT_CHECKPOINT_V2.12.txt

Whole-project descriptive background only:
- LOCALBOT_PROJECT_WHOLE_SUMMARY_V1.7.txt

Concept background only:
- Notes_ConceptsV2.0.txt


FILE AND VERSION HYGIENE RULE

This rule protects the project folder from clutter and naming drift.

Root-name rule:
- keep the root file name stable
- when updating an existing file, change only the version number
- do not add role suffixes such as ARCHIVE_REFERENCE, CLEANED, FINAL, NEW, or FIXED to files that already have a home

No-clutter rule:
- do not create ZIP packs or side files unless the user asks or the material has no existing file home
- if the content belongs in master, unified, checkpoint, phases, concepts, machine reference, roadmap, or helper context, update that existing root file with the next version
- do not resend unchanged files just to look complete
- do not send updated files to the user unless the user asks for files or the current task explicitly requires a file deliverable
- if the user says to note, remember, lock, or hold something without asking for files, keep the note in working context until a file update is requested

Confusion-prevention rule:
- if an older file could confuse the current direction, fix its role wording inside the same root file and bump the version
- do not solve role confusion by changing the root file name

PHASE-END DOCUMENTATION RULE

After each phase is completed, the assistant must ask this prompt before moving on:

Update the handoff stack now? (Y/N)

If Y:
- update the master handoff if current control truth changed
- update the unified handoff with new verified context, history, or checkpoint results
- update concept notes only if new background material was actually learned

If N:
- record the phase result clearly enough in the current working context
- do not force a documentation pass
- move cleanly to the next practical step

CURRENT PHASE STATE

PHASE 1:
COMPLETE

PHASE 2:
COMPLETE

PHASE 3:
COMPLETE

PHASE 4:
ACTIVE

Important:
Phase 4 is not closed.
The working loop is already proved.
The current job is to keep Phase 4 practical, stable, and machine-fit.

BEST CURRENT LIVE CHECKPOINT

The following were confirmed directly in terminal during the latest locked checkpoint.

config.txt:
MODEL_NAME=qwen2.5-coder:3b
WORKER_TIMEOUT_SECONDS=120
RECOVER_RUNNING_TO_PENDING=1

Installed models:
- roblox-bot:latest
- qwen2.5-coder:1.5b
- qwen2.5-coder:3b

Important:
- qwen2.5-coder:3b is the current locked daily default
- do not reopen model comparison casually
- only reopen if fresh live machine evidence justifies it

CURRENT TASK-FAMILY DECISION

Current primary Phase 4 build lane:
- roblox_work_order_builder

Accepted MVP purpose:
- take a structured Roblox Studio work order
- generate an executable Roblox Studio Luau builder script
- generate a local report
- keep execution human-reviewed inside Roblox Studio

First target test case:
- MAP_BUILD_TEST_001_FOREST_CAMPSITE

Accepted task-type labels for the new lane:
- ROBLOX_WORK_ORDER_BUILDER
- Build-map-content-from-command-list

Legacy safe text lanes remain valid but are no longer the main project direction:
- rewrite existing short text
- clean up existing short text
- summarize existing short text
- classify short text
- lightly transform user-provided text while preserving source meaning
- source-tied tasks over existing LocalBot wording, outputs, or logs

Current practical read:
- the base loop works
- qwen2.5-coder:3b remains the locked daily default
- the project should not reopen model comparison casually
- the new useful target is not advice-only output
- the new useful target is controlled Roblox work-order execution through generated builder scripts

ROBLOX WORK ORDER BUILDER MVP RULES

MVP lane name:
roblox_work_order_builder

MVP output files:
- outputs/<TASK_NAME>_builder.lua
- outputs/<TASK_NAME>_report.txt

MVP approved generated Roblox namespaces:
- Workspace/GeneratedMap
- ServerScriptService/GeneratedMapScripts

MVP safety rules:
- do not delete existing user work outside approved generated namespaces
- do not write outside LocalBot outputs from the local generator
- do not claim Roblox Studio can write back to C:\LocalBot\outputs without a later bridge
- generate clear builder scripts and reports before attempting Rojo or plugin integration
- keep the user in control of running/testing the builder script inside Roblox Studio

MVP success condition:
- LocalBot reads the structured work order
- LocalBot produces a usable builder script
- the user runs it inside Roblox Studio
- Workspace/GeneratedMap is created or updated
- ServerScriptService/GeneratedMapScripts is created or updated
- required objects are verified
- Studio Output prints a clear LOCALBOT_BUILD_DONE or LOCALBOT_BUILD_FAILED message

Later-only extensions:
- Rojo project writer
- Roblox Studio plugin bridge
- direct apply/sync from LocalBot to Studio
- automatic playtest support
- Studio-to-LocalBot status reporting

FAILURE 9

Failure 9 = output shape fail.

Use this label when:
- the loop runs cleanly
- the task completes
- the output stays tied closely enough to source meaning
- the main failure is presentation shape, such as numbering, bullets, markdown-like prefixes, wrapper text, wrong line count, blank lines, or one-line collapse

Response rule for Failure 9:
- treat it as prompt adjustment only
- do not change code
- do not change config
- do not change model
- stay in the same safe task family unless fresh evidence shows broader drift

USER WORKFLOW RULES

The user is working live in Command Prompt.
They want practical help without being talked down to.
They can lose track when commands or file instructions get messy.

Use these rules:
1. One command per copy box during live terminal work.
2. When opening or creating task files, use:
   start "" /wait notepad "C:\LocalBot\tasks\pending\filename.txt"
3. A PC file path is not the same thing as a downloadable chat file.
4. If lines like "'No' is not recognized..." appear, prompt text was pasted into Command Prompt instead of Notepad.
5. If commands get mashed together in one line, that is usually a copy or paste terminal mistake, not evidence that LocalBot itself broke.
6. Reserve downloadable files for real checkpoints, handoffs, or direct requests.


NORMAL RUN START RULE

This rule defines the normal live run shape for the common practical Phase 4 loop.

Purpose:
- make the expected run shape immediately clear at checkpoint start
- keep the assistant from over-expanding the run flow during ordinary live work
- preserve the user’s normal operating rhythm

Default normal run shape:
1. Open or create the pending task file in Notepad.
2. Paste the task text into that file.
3. Run python coordinator.py
4. Check the task result state using the done and failed folders.
5. Read the newest output text.
6. Report those two readbacks back to the assistant:
   - task result state
   - output text

Normal live run command set:
- Notepad command
- python coordinator.py
- done/failed check
- newest output read

Important limits:
- do not pad the normal run with extra helper commands unless the user asks
- do not front-load config checks, model checks, folder inventories, or log reads into the ordinary run loop unless live evidence or the user’s request makes them necessary
- do not replace the normal run with a larger diagnostic flow unless there is a real reason



ROBLOX WORK ORDER BUILDER RUN RULE

This rule applies only to the Roblox work-order builder MVP.

Default MVP run shape:
1. Put one structured Roblox work order into tasks/pending.
2. Run the normal coordinator flow.
3. LocalBot generates outputs/<TASK_NAME>_builder.lua and outputs/<TASK_NAME>_report.txt.
4. User opens Roblox Studio.
5. User runs the generated builder script in Studio.
6. User checks Studio Output for LOCALBOT_BUILD_DONE or LOCALBOT_BUILD_FAILED.
7. User reports the Studio result back before any next tightening pass.

Important:
- this is still one LocalBot task at a time
- one task may contain many related Roblox build steps
- do not confuse "one task" with "one tiny object"
- the work order is the task packet

RUN CLOSURE AND FRESH-START RULE

This rule controls what happens after a run result is judged.

Pass closure rule:
- if a run passes
- treat that run as closed
- do not keep talking about it as an active second run or continuing branch by momentum
- the next task should start as a fresh new run by default

Default next-run label after a pass:
- Run: practical_xxx
- Branch state: first run
- Real phase: Phase 4 active

Exception:
- only keep a branch open after a pass if the user explicitly wants a continued comparison branch or a deliberate chained test

Failure continuity rule:
- if a run fails, partially succeeds, or is labeled Failure 9
- the assistant may continue the same branch when that is the clearest practical read

Interpretation rule:
- a pass ends the current run by default
- the next ordinary task starts fresh unless the user clearly asks otherwise
- this rule only governs run framing and branch continuity
- it does not change any other workflow, authority, or phase rule

RULE ISOLATION

This rule is only about the default shape of the normal live run.

It does not:
- change authority order
- change the real project phase
- change the safe task-family decision
- remove the one-command-per-copy-box rule
- remove the step-by-step response style when that style is being used
- force extra diagnostics into ordinary runs
- prevent expanded troubleshooting when actual problems appear
- let pass-state confusion carry into the next run

Interpretation rule:
Use the normal run start rule as the default practical run shape.
Use larger command sets only when the user asks for them or when real debugging evidence justifies them.

DEFAULT RESPONSE STYLE RULE

For live project work, the assistant should respond in the same step-by-step style by default.

Use this style:
- present the work as clear numbered steps
- keep one command per copy box
- separate paste text from commands
- keep the order explicit
- keep the flow steady and easy to follow

STATUS UPDATE RESPONSE RULE

When the user says "status update" or asks where the project stands, return a clean project-progress readback.

Use this structure unless the user asks for a different format:

1. Overall progress read
- include a compact table with areas, status, and percentage estimates
- separate the old working LocalBot core from the newer Roblox Work Order Builder lane

2. Big-picture number
- give one honest approximate percentage for the whole project including the new Roblox builder direction
- make clear when that number is uneven because one part is mature and another part is early

3. What is strong already
- summarize what is working or already proven

4. What is weak right now
- summarize the current bottleneck or unproven area

5. Current real bottleneck
- state the actual blocker in plain language

6. What probably needs to happen next
- list the next necessary build pieces or validation steps

7. Clean status read
- close with a short practical summary of where the project stands

Formatting rules:
- keep it clean but detailed enough to understand the project state
- do not put the whole status update in one code box
- use tables where they improve readability
- use code/text boxes only for exact values, commands, or copy/paste material
- include percentages as practical estimates, not false precision
- do not turn a status update into a command run unless the user asks for commands

RUN LABEL HEADER RULE

During live multi-run revision loops, put a short run label header at the top before the step list.

Purpose:
- help the user see where the branch currently is
- reduce drift anxiety
- reduce spaghetti / looping confusion
- keep the run identity explicit without adding a long explanation

Use this compact shape:
- Run: practical_xxx
- Branch state: first run / second run / reset-baseline run / special run
- Real phase: Phase 4 active

Important:
- keep it short
- do not turn it into a long note block
- place it above the steps, then continue normally
- if the user says not to include it for a specific reply, follow the user
- after a pass, reset the next run to a fresh first-run label unless the user explicitly keeps the branch open

LOCAL TWO-PASS REVISION RESET RULE

This is a special workflow rule for the local revision loop only.
It does not change the real project phase state.

Meaning here:
- local Phase 1 = the first pass or stronger baseline in a small revision branch
- local Phase 2 = the second pass in that same revision branch

Rule:
- if both local passes have been used
- and the branch still is not good enough or still does not pass
- reset back to the stronger local Phase 1 baseline
- do not keep stacking constraints onto the weaker branch
- rebuild from the stronger earlier point instead

LOCAL SECOND-PASS HOLD RULE

This rule applies only inside the local two-pass revision workflow.

Rule:
- when a proposed second-pass revision is presented
- assess it internally first
- note what you might have changed or questioned
- hold those thoughts
- do not rewrite the proposed revision before the run
- send the first path exactly as provided for execution
- wait for the result
- only after the result returns, compare the held pre-run judgment with the actual post-run result

SPECIAL MODE LOCK AND SILENT HOLD RULE

This rule controls special-run collaboration when another assistant returns a candidate, strategy, code, prompt, or file change to test.

Mode trigger:
- If the user says SPECIAL, SPECIAL 1a, special run, or sends material headed with a SPECIAL marker, treat that as an active mode lock.
- If the current mode is SPECIAL 1a, returned assistant work is a run candidate, not a review request.
- Casual discussion does not cancel the active special mode unless the user clearly changes mode.

SPECIAL 1a allowed behavior:
- silently inspect the returned work
- internally note what may work or fail
- hold that pre-run judgment silently
- run the returned solution as provided
- wait for actual run evidence

SPECIAL 1a not allowed before run evidence:
- do not say PASS or FAIL
- do not critique the returned work out loud
- do not rewrite it
- do not improve it
- do not summarize it as a verdict
- do not add strategy commentary unless the user explicitly asks outside the run

Safety exception:
- If the returned work is unsafe, impossible to run, destructive, or clearly violates the user's file rules, stop and report that specific blocker.
- Do not use this exception for ordinary disagreement or preference.

After run evidence returns:
- then report PASS or FAIL if the user expects a special-run report
- compare the held pre-run assessment against the actual result
- state whether the earlier read was confirmed, partly wrong, or replaced by a different pattern
- only then choose the smallest justified next move

Held-thought disposal:
- if the run passes and held concerns no longer matter, set them aside
- if the branch closes, set unused held concerns aside unless they are needed for the final read
- do not dump hidden concerns just to show reasoning

SPECIAL RUN CLOSED-LOOP RULE

This rule applies only to the special local revision run workflow.

Closed-loop rule:
- if a special run is not lockable
- repeat the special run loop instead of stopping immediately

Escalation rule:
- if the branch starts to spaghettify
- or clearly looks like it is getting out of control
- stop tightening the current branch
- review the progression from the very start of the run chain
- compare all major runs in the chain, not just the latest one
- identify the strongest checkpoint, the main regressions, and the real pattern
- then choose the next route from that full-chain read


SPECIAL RUN RESULT REPORTING RULE

This rule applies to live special runs and repair branches.
It does not replace the phase-end documentation rule or the handoff stack rule.

Required report shape after the user returns run evidence:

1. First line:
- PASS
or
- FAIL

2. Then use these sections when applicable:
- Result received
- Before-result read
- After-result read
- Comparison
- Verdict
- What I think happened
- Rule issue found
- Decision / next move

Formatting rules:
- do not wrap the whole report in one code box
- do not put the whole response in one code box
- use code boxes only for exact console output, exact task state, exact output text, commands, code snippets, or Notepad paste blocks
- a large code/text box is allowed only when the user needs to copy one complete block into Notepad or into a file
- do not add a separate "handoff" block unless the user explicitly asks for one
- do not tell the user what another assistant needs to know unless the user asks for that format
- keep the report clean and directly tied to the run evidence

SPECIAL RUN BEFORE/AFTER COMPARISON RULE

Before fixing a failed special run, compare the pre-run read against the returned evidence.

Required comparison behavior:
- state what was expected or suspected before the run
- state what the actual evidence proves after the run
- say whether the earlier read was confirmed, partly wrong, or replaced by a different pattern
- only then choose the next move

Do not jump from failure directly into a new fix unless the user explicitly asks for commands only.

SPECIAL RUN COMMAND-ONLY RULE

If the user asks for commands only:
- give the compact run label header if useful
- send only the needed commands and paste text
- keep one command per copy box
- keep paste text separate from commands
- do not explain the steps

SPECIAL RUN TWO-RUN CAP RULE

If the user explicitly gives a two-run cap:
- run once
- inspect the result
- run one controlled repair only if justified
- if the second run also fails, stop tightening that branch
- do not freestyle a third repair
- wait for the next SPECIAL input or the user's next direction

This user-given two-run cap overrides the older closed-loop habit for that specific branch.


ISSUE RUN PHASE / SPECIAL 2A-2B RULE

This rule controls heated or confusing run loops without changing the real project phase.

Real phase rule:
- the real project phase remains Phase 4 unless the phase file says otherwise
- issue-run phases are local labels for one problem branch only
- an issue can reset back to Run 1 / Phase 1 with all learned notes carried forward

Universal issue-phase rule:
Every serious issue should be handled like the run/phase system, not as loose discussion.

For each issue, keep these clear:
- issue name
- layer being tested
- current local issue phase
- exact input
- exact proof/output
- pass condition
- fail condition
- what changed from the last attempt
- comparison across attempts when more than one attempt exists
- final call
- carry-forward lesson for the next local phase

Purpose:
- peel the stack apart cleanly
- gather useful evidence
- avoid smashing unrelated fixes together
- rebuild the stack in the intended order after the layer is understood

Reference-file application:
- the issue-phase discipline applies to reference files too, but only inside each file's proper role
- reference files must not become active control files just because an issue touches them
- specs and machine-reference files apply the process only to machine-fit or environment evidence
- roadmap files apply the process only to base/core-loop design and rebuild reasoning
- summary files apply the process only to historical lessons and progress-trail interpretation
- concept notes apply the process only to reusable technical concepts, trade-offs, and patterns
- persona files are special-treatment files and should not be changed unless the user explicitly asks or behavior rules are directly wrong
- if a reference file needs an issue-phase note, keep it role-safe and avoid live commands, active run instructions, or current control decisions

Special 2a rule:
Special 2a is the comparison loop proven by the Roblox builder diagnostic round.

Use Special 2a when there are multiple candidate reads or fixes and the branch needs data, not argument.

Special 2a sequence:
1. Run the first candidate exactly as provided.
2. If it is not perfect, run the revised candidate exactly as provided.
3. If it is still not perfect, run the main assistant attempt.
4. Then compare all attempts before deciding the next route.

Special 2a command behavior:
- when it is time to run, send commands only in the normal run-command style
- do not mix broad reasoning into the command block
- read exact target output files, not generic newest *.txt
- for LocalBot task outputs, prefer *_output.txt for artifacts and *_status.txt only for state
- do not treat a status file containing done as the artifact

Special 2a result behavior:
- after each run result, report what happened cleanly
- after the final attempt in the 2a sequence, compare all attempts
- make a call from the evidence, not from only the newest result
- classify results honestly as perfect, good, partial, or fail

Special 2b rule:
Special 2b must work from the Special 2a comparison, not restart blindly.

Use Special 2b when Special 2a still does not fully solve the issue.

Special 2b sequence:
1. Review the Special 2a attempt chain.
2. Identify what each attempt improved, broke, or proved.
3. Convert the learned evidence into the next clean Phase 1 / Run 1 setup for that issue.
4. Do not run another candidate until the new Phase 1 setup is clear or the user says to run it.

Special 2b hard stop:
- if Special 2b still cannot shake the issue out, stop running
- do not create Special 2c by momentum
- review the full progress trail before any more attempts
- explain how A led to B and how B led to C
- separate evidence from assumptions
- decide whether the next route is prompt, code, output-path, workflow, timeout/model, or architecture/design

Meaning:
Special 2a gathers comparative evidence.
Special 2b converts that evidence into a reset or a full trail review.

EXTERNAL ASSISTANT RETURN RULE

When the user brings back work from another assistant:
- do not rewrite it before the run
- do not touch files that the user said not to touch
- inspect it mentally and hold the pre-run judgment
- try the returned work as provided unless it is unsafe or directly violates an active project rule
- after the result returns, compare the held pre-run judgment with the real evidence
- only after that comparison choose the smallest justified correction


RECENT VERIFIED PRACTICAL CHECKPOINTS

practical_055
- completed
- Failure 9
- numbering leak only
- source meaning stayed close
- prompt adjustment only

practical_056
- completed
- passed
- exact 5-line rewrite held
- numbering fixed with prompt adjustment only

practical_057
- completed
- passed
- classification lane held again
- exact allowed-label output held

practical_058
- completed
- failed
- summary drifted beyond source meaning

practical_059
- completed
- failed
- tighter summary still drifted
- local Phase 2 failed in that summary branch

practical_060
- completed
- failed
- reset-baseline summary run also failed
- summary branch became the special-run handoff case

practical_061
- completed
- Failure 9
- rewrite wording stayed close
- output collapsed into one line

practical_062
- completed
- passed
- exact 5-line rewrite restored
- stronger rewrite baseline confirmed

practical_063
- completed
- partial success
- structure held
- punctuation cleanup still weak

practical_064
- completed
- stronger partial success
- punctuation cleanup improved
- meaning-preservation collision remained around "now"

practical_065
- completed
- partial success
- "now" preserved
- comma cleanup still weak

practical_066
- completed
- passed
- stricter cleanup rewrite became lockable

practical_067
- completed
- Failure 9
- blank-line regression
- worse than practical_066

practical_068
- completed
- passed
- blank-line regression fixed
- practical_066 quality regained


practical_069
- completed
- Failure 9
- summary-style compression returned
- not lockable against the stronger exact 5-line rewrite lane

practical_070
- completed
- passed
- exact 5-line rewrite restored
- stronger rewrite baseline re-confirmed

practical_071
- completed
- Failure 9
- cleanup task collapsed into one wrapped paragraph
- shape miss only

practical_072
- completed
- passed
- reinforced anti-merge rewrite held
- exact 5-line shape restored again

practical_073
- completed
- partial success
- cleanup shape held
- punctuation cleanup still leaked on failed tasks and new outputs lines

practical_074
- completed
- worse partial success
- exclamation cleanup improved
- comma leak remained
- final period on coordinator.log regressed
- do not carry forward

practical_075
- completed
- passed
- reset-baseline cleanup repair worked
- exact 5-line cleanup held with period and comma rules restored

practical_076
- completed
- passed
- classification lane held again
- exact one-label output "log" held

practical_077
- completed
- Failure 9
- numbering returned
- wording drifted away from the source
- rewrite lane needed a tighter prompt-side repair

practical_078
- completed
- passed
- rewrite lane repaired cleanly
- exact 5-line source-tied rewrite restored

practical_079
- completed
- passed
- classification lane held for source "outputs" -> "output"

practical_080
- completed
- failed
- classification miss
- source "failed" incorrectly returned "output"
- not a shape fail

practical_081
- completed
- passed
- classification repair held
- exact-repeat rule for allowed labels fixed the weak point

CURRENT DIRECTION

Prefer next:
- build and test the Roblox Work Order Builder lane as the main Phase 4 direction
- keep the existing LocalBot core unchanged unless live code evidence forces a change
- start with builder-script generation, not direct Studio control
- use MAP_BUILD_TEST_001_FOREST_CAMPSITE as the first structured test work order
- produce a Luau builder script plus a local report
- keep all generated Roblox map content inside Workspace/GeneratedMap
- keep generated Roblox scripts inside ServerScriptService/GeneratedMapScripts
- add validation and safety checks before adding more automation

Avoid next:
- trying to make LocalBot fully control Roblox Studio before the builder-script MVP works
- adding Rojo or a plugin bridge before the generated-script path is proved
- changing model/config/code from notes alone
- expanding into vague full-game generation
- deleting or overwriting user work outside generated namespaces
- creating scattered new files when existing handoff/concept/checkpoint files already have a role

Current practical read:
- rewrite/cleanup/classification history remains useful background
- the previous advice-only task lanes are no longer the main build target
- the new target is controlled Roblox work-order execution
- the strongest first proof is a generated Studio builder script that creates and verifies the forest campsite test map

MOVING FORWARD DECISION

Default forward route:
- treat the existing LocalBot base loop as stable
- add the Roblox Work Order Builder as a Phase 4 lane
- do not rebuild LocalBot from scratch
- do not update unrelated files
- keep rules/control truth in the master handoff
- keep broader context/history in the unified handoff
- keep quick current state in the checkpoint
- keep concepts/logic/options in the concept notes
- only create new files when they add integrity, such as a generated builder script, a report, or a clearly named test task

Why this route is preferred:
- it protects the existing stable loop
- it gives LocalBot real work to do instead of only advice
- it keeps the implementation realistic for the current Windows PC
- it avoids early overreach into live Studio control
- it creates a clear path toward later Rojo or Studio-plugin integration

Alternatives considered:
- keep LocalBot as advice-only
- jump directly to a Studio plugin bridge
- rebuild the whole architecture around Roblox
- make a broad autonomous Roblox game builder

Current read of those alternatives:
- advice-only is too weak for the user's goal
- direct plugin control is attractive later but too much for the first proof
- rebuilding the whole system would violate the protect-what-works rule
- broad autonomous game building is too risky and vague for this project stage

CURRENT IDEA-SHAPE WEAK POINTS

At the current stage, the weakest parts of LocalBot are not the base loop.
The new weak points are integration and output usefulness.

Current weak points:
- LocalBot does not yet generate executable Roblox builder scripts
- the structured work-order format must stay strict enough to parse
- generated scripts need safety guards before they are trusted
- Roblox Studio cannot report directly back to C:\LocalBot\outputs without a later bridge
- color names, object paths, script types, and service names must be normalized deterministically
- one work order may contain many build steps, so reports must stay clear
- the project can still drift if it tries to become a full autonomous Studio controller too early

Protective fixes that now matter:
- start with one known test work order
- use a generated Luau builder script as the first bridge
- keep all generated map objects under Workspace/GeneratedMap
- keep generated scripts under ServerScriptService/GeneratedMapScripts
- print clear Studio output status
- write a local generation report
- only add Rojo/plugin/direct testing after the builder-script path works

FIRST CHECK FOR THE NEXT ASSISTANT

Before any major code/build decision, verify live state instead of trusting notes alone.

Minimum check if working on the existing LocalBot runtime:
type config.txt

Optional if model inventory matters:
ollama list

If config.txt still shows:
MODEL_NAME=qwen2.5-coder:3b
WORKER_TIMEOUT_SECONDS=120
RECOVER_RUNNING_TO_PENDING=1

then keep qwen2.5-coder:3b as the daily default and do not reopen model comparison casually.

If working on the Roblox Work Order Builder:
- keep it as a Phase 4 lane
- do not rebuild the base loop
- start with structured work order -> generated builder script -> user runs in Roblox Studio
- use MAP_BUILD_TEST_001_FOREST_CAMPSITE as the first test case
- do not claim direct Studio control until a Rojo or plugin bridge is actually built and proved