# Helper Front Door Current 20260530

Status: CURRENT HELPER POINTER / READ-ONLY CHECKLIST / NOT DOCTRINE
Authority: not ACTIVE_GUIDES; not CURRENT_TRUTH_INDEX; not automation
Purpose: name the current helper doorway so helpers reduce live-chat burden without using stale paths or false PASS.

## Canonical Active Root

Use this as the current active helper command-center root:

```text
C:\Users\13527\Desktop\123\Jxhnny_Kl33N_Seedz\COMMAND_CENTER
```

Use this as the current child-shell root:

```text
C:\Users\13527\Desktop\123\Jxhnny_Kl33N_Seedz\COMMAND_CENTER\CHILD_SHELL
```

Archived/local-custody command-center material lives here:

```text
C:\Users\13527\Desktop\123\_LOCAL_CUSTODY_AND_RECEIPTS\COMMAND_CENTER\ROOT_COMMAND_CENTER_20260530
```

Archive/custody material may provide proof history, reports, and patterns. It is not the active helper execution root unless a task explicitly says so.

## Stale Path Warning

Treat scripts that target this old path as stale until path-repaired:

```text
C:\Users\13527\Desktop\123\COMMAND_CENTER
```

Reason:

```text
The root cleanup condensed the loose root command center into local custody.
The active-looking command center is inside the repo room.
Running old-path scripts can create missing-folder failures or duplicate command-center surfaces.
```

Legal verdict for an old-path helper:

```text
STALE PATH WARNING / DO NOT RUN UNTIL PATH-REPAIRED
```

## Approved Bounded Mechanisms To Consider

Use the smallest mechanism that proves the job:

```text
Level 0 - no helper: direct judgment or tiny answer.
Level 1 - checklist/rubric: helper burden value, proof need, boundaries.
Level 2 - read-only counter/search: bounded roots, limits, report path.
Level 3 - guarded local helper: Code Gate or child-shell runner, one job.
Level 4 - save/receipt script: only when Git/save/custody proof is the task.
```

Known useful mechanisms:

```text
COMMAND_CENTER\CHILD_SHELL
MULE_WORKSHOP\ASSISTANT_TO_MULE and MULE_TO_ASSISTANT packets
_TOOLS_AND_SCRIPTS\SOURCE_TO_HOUSE_INTAKE_FAMILY_20260530
_TOOLS_AND_SCRIPTS\SAVE_SCRIPTS\ROOT_CLEANOUT_SCRIPTS_20260530_200709
_TOOLS_AND_SCRIPTS\ROOT_TOOL_PACKAGES_20260530\BATCH_CONTENT_SEARCH_HELPER_V1.1
_LOCAL_CUSTODY_AND_RECEIPTS\COMMAND_CENTER\ROOT_COMMAND_CENTER_20260530\HELPER_GROWTH_REPORTS
```

These are not blanket permission to run. They are candidate mechanisms to route through the current task boundary.

## Helper Operating Order

Default order:

```text
one active object
one bounded helper packet
one toolbelt
one proof pointer
one return path
one stop condition
one recorded result
reset before the next packet
```

Helpers may run two or three at once only when the tasks are independent and do not share write scope. Otherwise, run one clean helper step at a time.

## Required Helper Return

Every helper return should include:

```text
Situation:
ProofObject:
ProofObjectLoaded:
FilesRead:
FilesWritten:
Boundary:
Warnings:
Verdict:
NextSafeAction:
DoNotTouch:
ReturnPath:
```

Allowed verdicts:

```text
RUN PASS
OUTPUT PASS
JOB PASS
HOUSE PASS
PASS WITH WATCH
NOT PASS
MISSING BONE
STALE PATH WARNING
BLOCKED
```

Hard latch:

```text
No BONE / no state claim.
RUN PASS is not HOUSE PASS.
Archive pattern is not active-root proof.
```

## Stop Conditions

Stop and report before running or expanding if:

```text
the helper points at C:\Users\13527\Desktop\123\COMMAND_CENTER;
the task needs Git, delete, move, network, watcher, or automation;
the helper cannot name its proof object;
the helper would broad-crawl without explicit root and limits;
the helper output would write outside its named output root;
the helper cannot say what next surface receives the handoff.
```

## Next Clean Helper Move

Before using local helper scripts, do one of these:

```text
1. Pick a current helper task that needs only read-only evidence.
2. Name the active root and output root.
3. Check whether the selected script uses stale root paths.
4. If stale, path-repair the script as a separate bounded task before running it.
5. Require a return with ProofObject and ProofObjectLoaded.
```

Boundary:

```text
This file is a pointer/checklist only.
It does not run helpers.
It does not promote helper registry repair.
It does not authorize broad helper swarm.
It does not edit ACTIVE_GUIDES or CURRENT_TRUTH_INDEX.
```
