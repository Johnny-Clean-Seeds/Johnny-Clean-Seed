# New Issues 1 Local Source / Checkpoint Review

Status: PASS WITH GUARDRAILS / LOCAL SOURCE CUSTODY + CHECKPOINT GRAMMAR APPLIED
Date: 2026-05-24

## Scope

This pass handled the second loose root issue drop as source ore, not as authority.

The pass also checked whether the suggested parent-to-smaller-station logic should become a full new gate, a checkpoint grammar, a tool, a card, parking, or a receipt-only item.

## Boss Loop 1 - Source Custody

Result: ADOPT AS LOCAL CUSTODY RULE.

Findings:

- Loose issue drops were moved out of root and into the local-only idea cell with a move ledger and receipt.
- Raw source and transcript material should default local-only from here forward.
- Existing tracked transcript surfaces were inventoried, not moved, because the active instruction said to leave existing Git evidence for now.
- The local-only idea cell is readable to the assistant. Local-only means not Git-facing, not hidden.

Failure route caught:

- If raw source material keeps getting saved into Git-facing notes, the repo becomes a heavy evidence warehouse instead of a clean assistant build surface.

Clean fix applied:

- Added a local source and transcript intake revision pass rule.

## Boss Loop 2 - Parent / Checkpoint Grammar

Result: ADAPT AS CHECKPOINT GRAMMAR.

Findings:

- The idea is useful, but it is not a reason to create another major gate.
- Full gates stay as rooms with distinct jobs.
- Smaller support checks attach to a parent gate, inherit that parent's trigger, and prepare output for the next parent gate.
- CONSOLIDATOR needed a label for this middle state.

Clean fix applied:

- Added the CONSOLIDATOR label `PLACE AS CHECKPOINT`.
- Added a Gate Family Checkpoint Card template.

Failure route caught:

- Without this label, the house has to choose between "make a full gate" and "park it," which creates either bloat or lost useful checks.

## Boss Loop 3 - Recursive Fit / Same-Issue Re-Run

Result: CLOSE ENOUGH TO USE, STILL WATCH.

Current recursive shape:

`parent gate -> checkpoint checks parent output -> parent confirms or narrows -> next gate judges the changed variant -> proof or parking captures residue`

Applied to this issue:

1. Source custody received the loose drop.
2. Revision pass separated source flavor from usable house logic.
3. Gate Inventory typed the usable object as checkpoint grammar and local source intake rule.
4. CONSOLIDATOR received the missing middle label.
5. Parking/TODO captured what still needs live use.

This is recursive in the useful sense: each gate judges the changed output from the prior gate instead of re-judging the original mess.

## Decisions

| Item | Decision | Reason | Return Trigger |
|---|---|---|---|
| Loose issue drops | ADOPT LOCAL CUSTODY | Root source drops should not stay loose. | Any future root issue/source drop. |
| Raw source/transcript evidence | ADOPT LOCAL DEFAULT | Keeps Git smaller and cleaner. | User asks for public/Git artifact. |
| Parent/checkpoint grammar | ADOPT SUPPORT GRAMMAR | Prevents fake full gates and lost small checks. | Any candidate is useful but too small for a full gate. |
| CONSOLIDATOR middle label | ADOPT AS `PLACE AS CHECKPOINT` | Needed to classify useful attached checks. | Any consolidation card sees useful smaller logic. |
| Existing tracked transcript files | WATCH / DO NOT MOVE NOW | Current instruction said leave existing evidence for now. | Separate explicit transcript demotion or repo cleanup order. |

## Blocked / Not Done

- Did not move existing tracked transcript files out of the repo. That would be a broad tracked-file change and was not necessary for this pass.
- Did not move the root local transcript folder. It is already local; this pass created a local evidence manifest instead.
- Did not add raw source text to Git-facing files.
- Did not touch ACTIVE_GUIDES, CURRENT_TRUTH_INDEX, doctrine, Git, or public authority.

## Next Clean Use

Use the checkpoint card on the next candidate that feels like a gate but may only be a support station.

Use the local source intake pass before any transcript/source idea crosses into Git-facing support logic.
