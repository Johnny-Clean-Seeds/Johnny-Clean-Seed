# MULE HANDOFF — ACTIVE MESS RULES / ROW 001 / PACKAGING RULE

Date: 2026-06-04
Status: MULE WORK ORDER / APPLY-NOW / REVIEW AFTER COMPLETION
WorkKey: MULE-HANDOFF-ACTIVE-MESS-RULES-ROW001-PACKAGING-20260604

## Prime order

Get to work on the active mess.

Do not stop at reading.
Do not only classify.
Do not only move files out of root.
Do not leave root messy.
Do not save files without making the good material ready for use.
Do not create nested zip packages.
Do not split related download/file links with little labels when reporting.

Required chain:

`LOOK -> ASK SHOULD THIS BE A LIVING RULE / CHECK / GATE? -> DECIDE -> DO WHAT NEEDS TO BE DONE`

Then:

`APPLY_ROOT_RULE_NOW -> CLASSIFY_ROOT_RESIDUE -> IDENTIFY_GOOD_MATERIAL -> TEST_OR_PROVE_GOODNESS -> INJECT_GOOD_INTO_CORRECT_PROJECT_LANE -> UPDATE_PATHS_POINTERS_MANIFESTS_STATUS -> FINAL_ROOT_NO_LOOSE_FILES_CHECK`

## Current known proof from user

User ran the packaging rule writer from:

`C:\Users\13527\Desktop\123`

Command used:

`pwsh -ExecutionPolicy Bypass -File "$env:USERPROFILE\Downloads\WRITE_PACKAGING_LINK_CLUSTER_RULE_20260604.ps1"`

Output:

`PACKAGING_LINK_CLUSTER_RULE_WRITTEN`

RunId:

`20260603_233310`

Rule path:

`C:\Users\13527\Desktop\123\Jxhnny_Kl33N_Seedz\BRAIN_LEARNING\PACKAGING_LINK_CLUSTER_AND_NO_NESTED_ZIPS_RULE_20260604.md`

Rule SHA256:

`789C82E2213B8BB9DA0E2D364F62CDB21DE2D3525510F51406C4805CC7A3D9BB`

Receipt path:

`C:\Users\13527\Desktop\123\Jxhnny_Kl33N_Seedz\PROOF_HISTORY\PACKAGING_LINK_CLUSTER_AND_NO_NESTED_ZIPS_RULE_RECEIPT_20260604.txt`

Receipt SHA256:

`5F5DA0C2E42F7137CF5ED7ED5FBEF4924E78BC79E6A0015B9F9DD9AADE6CB799`

Verdict from writer:

`PACKAGING_LINK_CLUSTER_RULE_APPLIED / NO_NESTED_ZIPS / NO_COMMIT / NO_PUSH`

Important: this rule is written locally in the repo working tree, but not yet committed or pushed according to the user paste.

## Active source objects likely involved

Known root-level loose objects reported earlier:

- `REPEATED_USER_CORRECTION_TO_LIVING_RULE_TRIGGER_V1_1.md`
  - SHA256: `4C951E17813ACFC5D9EF8F485A3B4648721959F7CC31AE915D3D14D1F03BC8F7`
- `ROW_001_DISPOSABLE_FIXTURE_DESIGN_V1.md`
  - SHA256: `6B52947701E4E5C94C17C7C77226E4F928D55B7E9A1FE637C83B6AA9B838F303`
- `SHA256_MANIFEST_ROW_001_DISPOSABLE_FIXTURE_V1.csv`
  - SHA256: `7E8B9E688BF9D4C34B9AF74ADE79C23199893AA02BEECC9597774AFB84EFED38`

Known root-level folders reported earlier:

- `ROW_001_DISPOSABLE_FIXTURE_V1`
- `MULE_HANDOFF_ROW_001_RULE_CAPTURE_20260604`

Likely additional objects may exist because correction packets were dropped after that. Inspect the actual current root.

Current root to inspect:

`C:\Users\13527\Desktop\123`

Current repo/home:

`C:\Users\13527\Desktop\123\Jxhnny_Kl33N_Seedz`

## Job 1 — apply root no-loose-files rule now

Before any other work, inspect the root.

Use judgment:

`ROOT_LOOSE_FILE_WRONG_LANE_UNTIL_PROVEN_ALLOWED`

Classify every loose root object as one of:

- `ALLOWED_ROOT_POINTER`
- `USER_DROP_INTAKE_PENDING`
- `WRONG_LANE_RULE_CANDIDATE`
- `WRONG_LANE_FIXTURE`
- `WRONG_LANE_HANDOFF_PACKET`
- `WRONG_LANE_CORRECTION_PACKET`
- `WRONG_LANE_MANIFEST`
- `WRONG_LANE_REPORT`
- `UNCERTAIN_PARK_DO_NOT_DELETE`
- `DO_NOT_TOUCH_USER_ORIGINAL`

Do not silently clean.
Do not delete user originals.
Do not leave new mule/helper files in root.
Do not claim clean closeout while loose wrong-lane root objects remain.

Required pass line after routing:

`ROOT_NO_LOOSE_FILES_CHECK_PASS`

Required blocked line:

`ROOT_LOOSE_FILES_PRESENT_CLOSEOUT_BLOCKED`

If wrong-lane material existed and was routed:

`WRONG_LANE_ROOT_RESIDUE_FOUND_AND_ROUTED`

## Job 2 — extract good material and inject it into the project

Root cleanup is not enough.

For each good object, do this:

1. prove it is good or useful;
2. place it in the correct project lane;
3. update paths/pointers/manifests/status references;
4. make the result ready for the next worker;
5. park or block anything unclear.

Required finish target:

`GOOD_MATERIAL_EXTRACTED_PROVED_INJECTED_PATHS_UPDATED_READY_FOR_USE`

Blocked if:

`ROOT_CLEANUP_WITHOUT_PROJECT_INJECTION_BLOCKED`

or

`MOVED_FILES_WITHOUT_PATH_UPDATES_BLOCKED`

Stop and ask if direction becomes unclear:

`DIRECTION_UNCLEAR_STOP_AND_ASK`

## Job 3 — parent repeated-correction rule

Candidate:

`REPEATED_USER_CORRECTION_TO_LIVING_RULE_TRIGGER_V1_1`

Core judgment:

`REPEATED_USER_CORRECTION_FORCES_LOOK_AND_RULE_DECISION`

Decision expected unless local review proves otherwise:

This should become a durable rule/check, because repeated user correction must force LOOK -> SHOULD THIS BE A RULE/CHECK/GATE? -> DECIDE -> DO WHAT NEEDS TO BE DONE.

Likely durable placement:

`BRAIN_LEARNING/REPEATED_USER_CORRECTION_TO_LIVING_RULE_TRIGGER_20260604.md`

Required work:

- inspect whether an older equivalent rule already exists;
- if none exists, save/route this as a durable rule candidate;
- if one exists, update/merge carefully or park with reason;
- create/update receipt;
- update status/path surfaces;
- include it in the save/commit set if the house save route is clean.

## Job 4 — child mule-root no-loose-files rule

Candidate:

`MULE_ROOT_NO_LOOSE_FILES_RULE_V1`

Core judgment:

`ROOT_LOOSE_FILE_WRONG_LANE_UNTIL_PROVEN_ALLOWED`

Decision expected unless local review proves otherwise:

This should become a durable rule/check and mule closeout gate.

Likely durable placement:

`BRAIN_LEARNING/MULE_ROOT_NO_LOOSE_FILES_RULE_20260604.md`

Also consider the current Mule Workshop / closeout checklist lane if one exists.

Required work:

- inspect whether an older equivalent rule/check exists;
- if none exists, save/route this as a durable rule/check;
- if one exists, update/merge carefully or park with reason;
- create/update receipt;
- update status/path surfaces;
- add or name the closeout gate;
- apply it to this work before closeout.

## Job 5 — Row 001 static fixture review

Target helper status remains:

`TARGET_HELPER_NOT_RUN`

`NO_TARGET_HELPER_RESULT_EXISTS`

Do not run the target helper.
Do not create a generated runner.
Do not execute a helper.
Do not claim helper pass/fail.

Objects to review/route:

- `ROW_001_DISPOSABLE_FIXTURE_DESIGN_V1.md`
- `ROW_001_DISPOSABLE_FIXTURE_V1`
- `SHA256_MANIFEST_ROW_001_DISPOSABLE_FIXTURE_V1.csv`
- any Row 001 static fixture packet dropped in root or handoff packet.

Likely lane:

`HOUSE_WORK/IDEA_CONCEPT_COLLECTION_ROOM/HELPER_STRESS_BENCH_ROW_001_STATIC_CODE_SHAPE_AND_FIXTURE_CARD_20260603/`

or the current best Helper Stress Bench Row 001 lane if local structure has changed.

Required fixture verification:

- fixture is inert;
- markdown/csv only;
- no `.ps1`;
- no `.cmd`;
- no `.bat`;
- no launcher;
- no watcher;
- no automation;
- no target-helper command;
- zip, if present, is inspected by listing only unless extraction lane is explicitly safe;
- do not extract zip into root.

Required Row 001 verdict:

`PASS_STATIC_FIXTURE_REVIEW`

or

`WATCH_STATIC_FIXTURE_REVIEW`

or

`BLOCK_STATIC_FIXTURE_REVIEW`

Required wording:

`RUNNER_LAYER_UNSTABLE`

`STATIC_FIXTURE_REVIEW_ONLY`

`TARGET_HELPER_NOT_RUN`

`NO_TARGET_HELPER_RESULT_EXISTS`

## Job 6 — packaging link-cluster/no-nested-zips rule

The user already ran the writer.

Rule exists at:

`BRAIN_LEARNING/PACKAGING_LINK_CLUSTER_AND_NO_NESTED_ZIPS_RULE_20260604.md`

Receipt exists at:

`PROOF_HISTORY/PACKAGING_LINK_CLUSTER_AND_NO_NESTED_ZIPS_RULE_RECEIPT_20260604.txt`

Current status:

`NO_COMMIT / NO_PUSH`

Required work:

1. Verify file exists.
2. Verify hashes if possible:
   - Rule SHA256 `789C82E2213B8BB9DA0E2D364F62CDB21DE2D3525510F51406C4805CC7A3D9BB`
   - Receipt SHA256 `5F5DA0C2E42F7137CF5ED7ED5FBEF4924E78BC79E6A0015B9F9DD9AADE6CB799`
3. Include it in the durable save/commit set if the save route is clean.
4. Do not create nested zip packages.
5. Use link clusters when reporting multiple files.

Packaging rule behavior:

When sending related files, cluster links together with no little labels between each link. Put explanation above and placement note below.

Construction rule:

No `.zip` files inside another `.zip` unless explicitly requested.

## Job 7 — save/commit/push only if clean and proved

If house save is part of the mule’s normal current job and the local repo is clean enough to proceed, use the standard proof route:

1. check Git state;
2. stage exact intended files only;
3. verify staged set;
4. commit with a clear message;
5. push;
6. verify `HEAD == origin/main`;
7. verify final status clean or explain remaining untracked/dirty items;
8. produce receipt/manifest.

Suggested commit title if clean and complete:

`Save repeated correction and root cleanup rules`

or stronger if Row 001 and packaging are included:

`Save correction rules and Row 001 fixture review`

Do not commit unrelated junk.
Do not commit root residue by accident.
Do not claim commit/push without proof.

If commit/push is not authorized or blocked, report:

`NO_COMMIT_NO_PUSH`

and explain the exact blocker.

## Required reports/receipts

At minimum, produce a report/receipt that states:

- root inventory before;
- root inventory after;
- classification of every root residue object;
- good material extracted;
- proofs/tests performed;
- destination path for each injected object;
- path/pointer/status updates made;
- objects parked and why;
- parent rule decision;
- child rule decision;
- Row 001 static fixture review verdict;
- packaging rule verification;
- target helper status;
- Git status before/after;
- commit/push proof or `NO_COMMIT_NO_PUSH`;
- final root verdict.

## Final answer shape required from mule

Use this shape:

`MULE_ACTIVE_MESS_WORK_COMPLETE`

or

`MULE_ACTIVE_MESS_WORK_BLOCKED`

Then list:

Root:
`APPLY_ROOT_RULE_NOW_DONE`
`WRONG_LANE_ROOT_RESIDUE_FOUND_AND_ROUTED` / `ROOT_NO_LOOSE_FILES_CHECK_PASS` / `ROOT_LOOSE_FILES_PRESENT_CLOSEOUT_BLOCKED`

Good material:
`GOOD_MATERIAL_EXTRACTED_PROVED_INJECTED_PATHS_UPDATED_READY_FOR_USE`
or blocked reason.

Parent rule:
`SAVED_PARENT_RULE` / `ROUTED_PARENT_RULE` / `PARKED_PARENT_RULE` / `ALREADY_EXISTS_APPLICATION_FAILED` / `BLOCKED_WITH_REASON`

Child rule:
`SAVED_CHILD_RULE` / `ROUTED_CHILD_RULE` / `PARKED_CHILD_RULE` / `ALREADY_EXISTS_APPLICATION_FAILED` / `BLOCKED_WITH_REASON`

Row 001:
`PASS_STATIC_FIXTURE_REVIEW` / `WATCH_STATIC_FIXTURE_REVIEW` / `BLOCK_STATIC_FIXTURE_REVIEW`

Packaging:
`PACKAGING_LINK_CLUSTER_RULE_VERIFIED`
`NO_NESTED_ZIPS_RULE_VERIFIED`

Target:
`TARGET_HELPER_NOT_RUN`
`NO_TARGET_HELPER_RESULT_EXISTS`

Git:
`COMMIT_AND_PUSH_PROVED`
or
`NO_COMMIT_NO_PUSH`

Final root:
`ROOT_NO_LOOSE_FILES_CHECK_PASS`
or
`ROOT_LOOSE_FILES_PRESENT_CLOSEOUT_BLOCKED`

## Hard stops

Stop and ask if:

- file ownership is unclear;
- moving a file risks destroying source custody;
- there are multiple plausible lanes with no clear winner;
- proof requires target-helper execution;
- a new generated runner would be needed;
- user originals are mixed with helper residue;
- a commit/push decision is ambiguous;
- the task starts expanding beyond root/rules/Row001/packaging.

Use:

`DIRECTION_UNCLEAR_STOP_AND_ASK`

Do not sprawl.

## Hard boundary

No generated runner.
No target helper execution.
No silent cleanup.
No nested zip packages.
No root dump into random folders.
No pathless save.
No fake ready claim.
No false clean closeout.
No deleting user originals.
No broad lane opening.
No doctrine promotion without normal proof path.
