# Helper Rubric Current Replay Plan 01

Date: 2026-05-28  
Status: CURRENT REPLAY PLAN / NOT A REPLAY RUN  
Authority: NOT doctrine; NOT ACTIVE_GUIDES; NOT CURRENT_TRUTH_INDEX  
Rubric source: `BRAIN_LEARNING/HELPER_BURDEN_VALUE_RUBRIC_20260528.md`  
Old sample score source: `BRAIN_LEARNING/HELPER_RUBRIC_OLD_TASK_SAMPLE_SCORE_01_20260528.md`  
Sample: old YouTube transcript / Desktop PowerShell task  
Placement: BRAIN_LEARNING  
Boundary: no replay execution, no scanner, no dashboard, no automation, no helper build.

---

## 1. Purpose

This plan defines the current minimum-helper replay shape for the old YouTube transcript / Desktop PowerShell task.

The goal is not to rebuild the transcript tool right now.

The goal is to define the clean current-way test so old flow can be compared against current minimum-helper flow.

---

## 2. Current Replay Thesis

The old task did not need a full repo save chain.

It needed a smaller helper level:

```text
Level 2 / Level 3 minimum-helper
```

Meaning:

```text
one local utility file
one clear run method
one Desktop output folder
one visible close marker
one narrow fix path
```

Do not use a full support-pack Level 4/5 unless the user explicitly wants the transcript tool saved as durable house material.

---

## 3. Replay Object

Task object:

```text
Create or run a simple YouTube transcript utility that:
1. asks for or accepts a pasted video URL,
2. gets transcript text where possible,
3. saves output to a visible Desktop folder,
4. gives the user a simple close message,
5. avoids buried paths and shell-paste confusion.
```

Current replay should measure whether minimum-helper flow reduces the previous burden.

---

## 4. Required Shape

The current replay must have:

```text
1. one downloadable `.ps1` file
2. one PowerShell command
3. one Desktop output folder
4. one expected close marker
5. no repo save unless separately requested
6. no dashboard
7. no scanner
8. no helper build
9. no broad framework
```

Preferred output folder:

```text
Desktop\YouTube_Transcripts
```

Preferred close marker:

```text
YOUTUBE_TRANSCRIPT_DESKTOP_TOOL_COMPLETE
```

---

## 5. Measurement Fields

Compare old sample against current replay using these fields:

| Field | Old Sample | Current Replay |
|---|---|---|
| User commands | many / unclear | one command |
| Script surface | chat/paste confusion | one file |
| Output location | unclear / buried | Desktop folder |
| Report close marker | none / weak | one marker |
| Repair path | conversational correction | narrow fix code if FAIL |
| Repo save | not needed | not used |
| User frustration | high | target low |
| Local proof | weak | run proof only |
| Helper level | Level 0 chaos | Level 2/3 minimum-helper |
| Risk of overbuild | n/a | watch: do not use Level 4/5 |

---

## 6. Current Replay Pass Conditions

The current replay would PASS if:

```text
the user can run one file or one command
the tool creates Desktop\YouTube_Transcripts
a transcript or clear failure note appears there
the output path is shown
the close marker is printed
no repo files are touched
no extra framework is created
```

---

## 7. Current Replay Watch Conditions

WATCH if:

```text
the transcript source blocks access
dependencies are missing
PowerShell execution policy interferes
the script grows too large
Code Gate warnings appear but target run passes
user still has to manually fix path/output confusion
```

WATCH is acceptable only if the user gets a clear output or clear failure note.

---

## 8. Current Replay Fail Conditions

FAIL if:

```text
the script requires manual line-by-line paste
output is buried
no Desktop folder is created
there is no close marker
the user cannot tell where the transcript went
the assistant explains instead of fixing
the helper becomes bigger than the task
```

---

## 9. Helper Value Prediction

Expected value:

```text
CURRENT MINIMUM-HELPER SHOULD HELP
```

Why:

```text
it reduces path confusion
it reduces shell-paste confusion
it gives a visible output folder
it gives a close marker
it gives one fix path
it avoids full repo/save overhead
```

Expected tax:

```text
one script
one command
possibly one Code Gate report
one Desktop output folder
```

Expected savings:

```text
less repeated correction
less user frustration
less live chat burden
less file-location confusion
cleaner handoff
```

---

## 10. Do Not Replay Yet Unless Selected

This plan does not run the replay.

Next selected move would be one of these:

```text
A. make the minimum-helper transcript utility file
B. make a tiny measurement comparison sheet from old-vs-current fields
C. stop here and return to user-directed work
```

Recommended if continuing this evaluation:

```text
A. make the minimum-helper transcript utility file
```

But only if the user still wants direct old-vs-current replay evidence.

---

## 11. Boundary

Blocked:

```text
no scanner
no dashboard
no automation
no helper build
no durable repo save of the tool unless selected
no doctrine promotion
no ACTIVE_GUIDES edit
no CURRENT_TRUTH_INDEX edit
no full support-pack ceremony
```

---

## 12. Verdict

```text
CURRENT REPLAY PLAN READY
MINIMUM-HELPER LEVEL 2/3 SELECTED
NO REPLAY RUN YET
NOT PROMOTED
```

