# Keyring Level 0 Use — Next Mechanism Selection

Date: 2026-05-28  
Status: LEVEL 0 KEYRING USE / SAVE-SELECTION CARD / NOT A BUILD  
Authority: NOT doctrine; NOT ACTIVE_GUIDES; NOT CURRENT_TRUTH_INDEX  
Source table: `BRAIN_LEARNING/HOUSE_KEYRING_GRAPH_ONE_CHAIN_TINY_PROOF_20260528.md`  
Purpose: use the Level 0 Keyring table once to orient the next bounded mechanism selection without building Level 1.

---

## 1. Clean Thesis

The Level 0 Keyring table is now used once.

This is not a Keyring expansion.

This is a proof of use:

```text
Can the saved one-chain table help choose the next mechanism route without rereading the whole run and without building Level 1?
```

Verdict:

```text
PASS CANDIDATE / LEVEL 0 TABLE USED ONCE / NEXT MECHANISM SELECTED / NOT PROMOTED
```

---

## 2. Source Object

Used object:

```text
BRAIN_LEARNING/HOUSE_KEYRING_GRAPH_ONE_CHAIN_TINY_PROOF_20260528.md
```

Relevant saved rule from that object:

```text
Use this Level 0 table once in a later orientation or save-selection task before any Level 1 movement.
```

This card performs that one use.

---

## 3. Boundary

Allowed:

- use the Level 0 table once;
- orient current chain;
- identify the strongest next bounded mechanism;
- save the selection card;
- keep proof and authority boundaries visible.

Blocked:

- no Level 1 Keyring movement;
- no full Keyring build;
- no registry;
- no dashboard;
- no helper/code build;
- no broad scan;
- no doctrine promotion;
- no ACTIVE_GUIDES edit;
- no CURRENT_TRUTH_INDEX edit;
- no authority-spine replacement.

---

## 4. What The Keyring Table Revealed

The Level 0 table made the chain readable without reopening the whole run.

It showed these live facts:

| Keyring Signal | Meaning |
|---|---|
| several objects are `READY_FOR_NARROW_USE` | proof chain is maturing but not promoted |
| several objects are `PROOF_PARK` | old proof should remain findable but not carried live |
| one object is `WATCH` | mechanism extraction remains source-selection pressure, not final law |
| current object is `CURRENT` | Keyring proof has just been saved |
| blocked-use lines are doing real work | they stop candidate objects from acting like doctrine |
| freshness column is load-bearing | stale/current/proof-park state prevents live-weight confusion |

The strongest gap exposed by the table:

```text
Freshness state needs its own tiny proof.
```

Reason:

The Keyring table worked partly because each row had a freshness state.

Without freshness labels, saved objects can drift into live instruction or disappear into old proof.

---

## 5. Candidate Mechanism Comparison

| Candidate Mechanism | Fit From Keyring Use | Risk | Verdict |
|---|---|---|---|
| Full Keyring Level 1 | tempting but premature | high authority drift | BLOCK |
| Registry seed | useful but too close to full inventory | high bloat / authority risk | PARK |
| Dashboard | visually useful but too early | high command-surface risk | BLOCK |
| Helper to format rows | convenient but unnecessary | tool before repeated need | PARK |
| House Walk Routine | useful but already manually active | medium scope creep | WATCH |
| Wayfinding Save-Selection | useful but overlaps this card | medium abstraction bloat | WATCH |
| Freshness / Decay Labels | directly exposed by table and small enough | low if bounded | SELECT |

Selected next mechanism:

```text
Freshness / Decay Labels Tiny Proof
```

---

## 6. Why Freshness / Decay Labels Win

Freshness labels answer:

```text
Is this object current?
Is it watch-only?
Is it proof-parked?
Is it ready for narrow use?
Is it superseded?
Is it stale?
Does it need review?
```

This is exactly what the Keyring table needed to stay safe.

Without freshness:

- old receipts look active;
- old candidate cards look like rules;
- current proof gets buried;
- stale proof remains live;
- helpers may reuse the wrong object;
- the assistant carries too much furniture instead of the key.

Freshness labels support live-load hygiene without deleting proof.

---

## 7. Tiny Proof Shape For Freshness

Next proof should be small.

Target object:

```text
BRAIN_LEARNING/FRESHNESS_DECAY_LABELS_TINY_PROOF_20260528.md
```

Tiny proof question:

```text
Can a small freshness label set reduce stale/live confusion across one saved chain without becoming a dashboard, registry, or authority layer?
```

Suggested label set:

```text
CURRENT
READY_FOR_NARROW_USE
WATCH
PROOF_PARK
SUPERSEDED
STALE
NEEDS_REVIEW
BLOCKED
LOCAL_ONLY
```

Proof surface:

Use the same recent saved chain already present in the Keyring Level 0 table.

Allowed output:

One candidate proof card and receipt.

Blocked output:

- no global relabeling;
- no full index;
- no dashboard;
- no helper;
- no authority edits;
- no ACTIVE_GUIDES;
- no CURRENT_TRUTH_INDEX;
- no doctrine.

---

## 8. Fit Decision

ADOPT:

Use Level 0 Keyring table as an orientation tool for this one save-selection task.

ADOPT:

Freshness / Decay Labels selected as the next bounded mechanism for tiny proof.

ADAPT:

Freshness labels must remain row-level status support, not authority.

PROOF:

This card proves the Keyring table can guide one next-route decision.

PARK:

Level 1 Keyring, registry, dashboard, helper formatter, broad graph, authority integration.

BLOCK:

- full Keyring build;
- Level 1 movement;
- registry build;
- dashboard;
- helper/code build;
- broad scan;
- doctrine promotion;
- ACTIVE_GUIDES rewrite;
- CURRENT_TRUTH_INDEX edit.

---

## 9. Pass / Watch / Fail

### PASS

This selection passes if:

```text
the Level 0 table helped choose one bounded next mechanism
the next mechanism is smaller than a build
the next mechanism is directly supported by table evidence
authority boundaries remain visible
```

### WATCH

Watch if:

```text
freshness labels start becoming a dashboard
labels are treated as command authority
labels multiply without proof
old objects are relabeled without a receipt
```

### FAIL

Fail if:

```text
this becomes Level 1 Keyring
this edits authority files
this starts a registry
this starts a dashboard
this starts a helper build
this rewrites history instead of labeling state
```

---

## 10. Final Judge Placement

Placement:

`BRAIN_LEARNING`

Authority:

Candidate save-selection only.

Final verdict:

```text
PASS CANDIDATE / LEVEL 0 KEYRING TABLE USED ONCE / FRESHNESS-DECAY LABELS SELECTED FOR TINY PROOF / NOT PROMOTED
```

---

## 11. One Next Route

Build:

```text
BRAIN_LEARNING/FRESHNESS_DECAY_LABELS_TINY_PROOF_20260528.md
```

Boundary:

One tiny proof card only.

No dashboard.
No registry.
No helper.
No full Keyring.
No doctrine.
No ACTIVE_GUIDES.
No CURRENT_TRUTH_INDEX.

