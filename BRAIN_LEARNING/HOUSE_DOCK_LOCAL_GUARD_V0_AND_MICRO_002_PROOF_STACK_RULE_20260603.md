# House Dock Local Guard V0 and Micro 002 Proof Stack Rule

Saved: 20260603_125519

## Verdict

HOUSE_DOCK_LOCAL_GUARD_V0_AND_MICRO_002_PROOF_STACK_SAVED

## What is locked

This save locks the support rule and proof stack for:

- House Dock Local Guard V0 Soft Suit behavior.
- Auto-fix intelligence as a growing repair ledger.
- AUTO_FIX_CANDIDATE_001_QUOTE_SAFE_BODY_WRITER.
- AUTO_FIX_WRONG_FIX_STOP_RULE.
- USER_ADDRESSED_RULE_SAVE_STACK_RULE.
- TRANSCRIPT_REPASTE_CONTAMINATION_RULE / OUTPUT_IS_NOT_INPUT_RULE.
- Micro 002 SHOW_TOOL_REGISTRY proof run.
- Micro 002 verifier repair V2 guard review.
- Micro 002 independent verification pass.

## Guard law

The guard must not merely fix surface-level issues.

Before accepting an auto-correction, it must run a lower-layer issue check. If the issue is deeper than a safe known correction, the lane must pause with:

PAUSE / LOWER-LAYER ISSUE MUST BE RESOLVED FIRST BEFORE CONTINUING THIS LANE

## Wrong-fix rule

If an auto-fix later proves wrong, overbroad, unsafe, misleading, or matched only by a vague label, it must stop and be marked:

WRONG_FIX_OR_OVERBROAD_FIX

Then the lane must identify the actual lower-layer issue, select or create the correct fix, and rerun proof before continuing.

## Auto-fix candidate 001

AUTO_FIX_CANDIDATE_001_QUOTE_SAFE_BODY_WRITER applies only to the proven class:

PowerShell report/body writers fail because markdown, delimiter examples, quotes, or backticks contaminate executable PowerShell strings.

Safe correction:

Use a quote-safe here-string or file-body writer for a new report/body file.

This fix must not be applied to all parser errors. A future parser issue must first match the exact lower-layer pattern and safety boundary.

## User-addressed rule save stack

When the user directly addresses an issue, correction, operating rule, or process rule, treat it as a lock/save candidate immediately.

Default route:

1. Apply it as active behavior now if it affects current work.
2. Stack it for the next available durable save/push location.
3. Do not interrupt the lane for immediate Git save unless not saving now would contaminate the active lane, the rule is required before the next command, the risk of loss is high, or the user explicitly says save/push now.

## Transcript repaste rule

Output is not input.

Prompt text, transcript lines, and status labels such as GUARD_RUN_ID, PASS, and PowerShell prompts are not commands and must not be pasted back into PowerShell as executable input.

## DoesNotProve

This save does not prove broad kernel safety.
This save does not prove UI readiness.
This save does not prove tool execution safety.
This save does not install public guard code.
This save does not authorize Micro 003.
This save does not authorize watcher, move/delete/cleanup, protected-path mutation, ACTIVE_GUIDES rewrite, CURRENT_TRUTH_INDEX rewrite, or doctrine rewrite.

## StopLine

Next move after this save is not Micro 003 by default. First close the save gate, confirm final clean push, then choose the next bounded implementation step.
