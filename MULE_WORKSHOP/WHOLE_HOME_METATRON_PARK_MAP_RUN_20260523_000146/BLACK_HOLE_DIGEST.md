# BLACK_HOLE_DIGEST

Status: digest after harvest, broadening, and priority / source preserved / not CURRENT

## Input Field

Absorbed:

- old history and closeout reports;
- good chip recovery;
- node and edge registries;
- algorithm fit matrix;
- word broadening report;
- current map and ledger, read-only;
- support mirrors, read-only;
- custody/source material pointers;
- dropped RECESS package.

## Dedupe

Merged duplicates:

- CURRENT user-only and READY-only prep are already active map rules.
- Old clean-bloat harvest and new Black Hole Digest are the same family, but the new package adds the correct order and named digest gate.
- Old word broadening and new broadening gate are the same family, but the new package adds failure-mode and bypass checks.
- Old parking/fit decision and new priority TODO are the same family, but the new package adds score, owner, and return trigger.
- Old Stop/Fix and new same-problem application are the same family.
- Old same-shape walk and new after-walk order are the same family.

## Clusters

| Cluster | Source Pull | Digest Output | Route |
|---|---|---|---|
| Authority and path | Old blockers + current map + new package | Path/home decision bell before READY/CURRENT-facing change | User Decision / Issue Review |
| RECESS order | New package + old clean-bloat gap | HPDR/MHBPkDARJ compact mode gate | Dressing Room candidate |
| Priority parking | New package + old candidate ledger | Priority TODO schema | Ledger candidate |
| Digest | New package + old dense extraction | Named Black Hole Digest stage | Map candidate |
| Broadening | New package + old word report | Alias/opposite/actor-lane-failure-bypass gate | Map/ledger candidate |
| Proof and judge | Old closeout + new labels | No PASS without route closure; REVIEW / NOT PASS now | Final Judge |
| Overflow | Algorithms, public URL, support mirrors, downloads | Set-aside with owner/priority/trigger | Priority parking ledger |

## Ranked Output

1. USER_DECISION_BELL: path/home clarity for accepted Soft Suit home.
2. DRESSING_ROOM_CANDIDATE: Soft Suit Dressing Room gate itself.
3. DRESSING_ROOM_CANDIDATE: RECESS mode order, compact form.
4. PRIORITY_TODO: priority parking schema.
5. DRESSING_ROOM_CANDIDATE: broadening gate and Black Hole Digest as merged compact rules.
6. MULE_REVIEW: support mirrors inside CURRENT, no move without user authorization.
7. PARK: public URL, algorithm overflow, ambiguous Downloads.

## Compression

Durable rule candidate:

`For RECESS or dense idea work: lock mode, harvest broadly, broaden variants, score priority, park overflow with owner and return trigger, run Black Hole Digest, test Soft Suit changes in the Dressing Room, prepare READY only if safe, return every object home, and final-judge without claiming PASS.`

## Source Archive

Source is not deleted. The dropped package remains at root. Old run files remain in place. This digest creates only review artifacts in the mule run folder.

## Return Home

Every candidate is routed to one of:

- user decision bell;
- dressing-room candidate;
- priority TODO;
- parked/set-aside;
- mule review;
- blocked;
- no action now.

