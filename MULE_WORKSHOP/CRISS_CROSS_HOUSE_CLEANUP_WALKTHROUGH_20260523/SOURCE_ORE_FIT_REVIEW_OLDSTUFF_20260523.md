# Source-Ore Fit Review — oldstuff.ps1

Status: SOURCE ORE FIT REVIEW / NOT RULES / NOT DOCTRINE  
Date: 2026-05-23  
Source object: oldstuff.ps1  
Related committed walkthrough: CRISS_CROSS_HOUSE_CLEANUP_WALKTHROUGH_20260523  
Boundary: this review does not execute the script, does not promote rules, does not rewrite authority, does not edit ACTIVE_GUIDES, does not edit CURRENT_TRUTH_INDEX, and does not adopt mule output as doctrine.

## 1. Intake Judgment

The source file is an old repair script for the criss-cross work order.

It must not be treated as a rule file.

It must not be run as-is.

It is useful only as source ore: compare it against the committed mule walkthrough, pull what fits, drop what is stale, reject bad names, and preserve only clean fit-tested ideas.

The prior Git state already saved the mule walkthrough return at:

`main @ 61d5cca`

Therefore the old script's original job — patching the work order before the mule run — is stale.

## 2. What Fits

### 2.1 Root-pressure mapping

Accepted as a lens.

Reason: the mule walkthrough found several related pressure branches, not one isolated problem.

Fit branches:

- path/home sync;
- pickup/root custody;
- proof/PASS validation;
- source custody;
- mule boundary;
- porch/mail lifecycle;
- parking and return triggers;
- resource/tool placement;
- local-only/private-like tracked path review;
- Work Shed reference mismatch.

Clean name:

`Root-Pressure Fit Lens`

Rejected name:

`MASTER ROOT-PRESSURE MAP — MUST GUIDE EXECUTION`

Reason: too authority-heavy for source ore.

### 2.2 Dependency order

Accepted as a review method.

Clean dependency order:

1. Path/home sync first, because wrong root corrupts every downstream file action.
2. Source custody second, because old files and old scripts must not become authority by accident.
3. Proof target third, because PASS must prove the intended target.
4. Mule boundary fourth, because mule output is review material unless separately adopted.
5. Porch/mail lifecycle fifth, because pickup and delivery surfaces must close cleanly.
6. Parking/return triggers sixth, because useful material dies without a return path.
7. Resource/tool placement seventh, because tools and scaffolds need the right home before reuse.

### 2.3 Per-anomaly proof chain

Accepted.

Every future criss-cross review item should answer:

- Which branch does this belong to?
- What downstream branch can it affect?
- Is the action fix-now, park, block, rename, link, or reject?
- What proof shows the decision is real?

This is not a new doctrine rule. It is a fit-review shape for this walkthrough family.

### 2.4 Resource / idea triage

Accepted.

Old notes, scripts, scaffolds, names, tools, and parked ideas should be sorted into:

- use now;
- right home;
- rename;
- link;
- park with return trigger;
- proof needed;
- block;
- reject.

This fits the current house need because the criss-cross walkthrough is about preventing useful material from staying scattered while also preventing bad/stale material from becoming active truth.

## 3. What Does Not Fit

### 3.1 Running the script

Rejected.

Reason: the script patches an old work order and the mule walkthrough already completed and was saved.

### 3.2 Retroactively changing required output count

Rejected.

The old script tries to add `MASTER_PRIORITY_DEPENDENCY_TREE.md` as a required file.

Reason: the committed mule return already saved the required walkthrough output set. Adding another required file after completion muddies proof.

Better action: if a dependency-tree addendum is needed, create it as a separate review addendum with a new proof boundary.

### 3.3 PASS language inside the old script

Rejected.

The script prints `PASS / PATCHED / NO GIT`.

Reason: that only proves a text patch ran. It does not prove the criss-cross walkthrough is complete, correct, or adopted.

### 3.4 Authority-heavy wording

Rejected.

Bad or risky names:

- `MASTER ROOT-PRESSURE MAP`;
- `MUST GUIDE EXECUTION`;
- `MASTER STACK`;
- `MASTER_PRIORITY_DEPENDENCY_TREE`.

Reason: they sound like doctrine or authority when this is only a source-ore review.

Clean replacements:

- `Root-Pressure Fit Lens`;
- `Dependency Review Shape`;
- `Source-Ore Fit Review`;
- `Criss-Cross Addendum Candidate`.

## 4. What May Have Been Missed In The House Walk

The committed walkthrough should be checked for whether each major anomaly has the full proof chain:

`branch -> downstream effect -> decision -> proof`

Likely review targets:

1. Old-home path references in `README.md`, `CURRENT_TRUTH_INDEX.txt`, `AGENTS.md`, and teleporter pointers.
2. Root pickup work order outside active-home pickup lane.
3. Prior `$Home` reserved-variable path bug evidence.
4. Work Shed references while active `HOUSE_WORK\WORK_SHED` is absent.
5. 122 tracked paths matching local-only/private/shed-like naming.
6. Root tracked helper/support files needing role review.

These are not automatically fix-now items. They are review targets.

## 5. Correct Next Use

Use this review only when deciding the next bounded criss-cross addendum.

Allowed next addendum types:

- path/home stale-reference review;
- local-only/private-like tracked path role review;
- Work Shed reference mismatch review;
- root pickup custody review;
- proof/PASS validation review;
- parking/return-trigger cleanup review.

Blocked:

- doctrine promotion;
- active guide rewrite;
- CURRENT_TRUTH_INDEX edit;
- delete action;
- Work Shed restoration;
- running oldstuff.ps1;
- accepting old names without rename review.

## 6. Final Judge

PASS for this source-ore review requires:

- oldstuff treated as source ore only;
- stale script behavior rejected;
- useful lens material extracted;
- bad names rejected or renamed;
- no authority changes;
- no script execution;
- no adoption of mule output as doctrine;
- next action remains bounded.

Verdict: PASS WITH GUARDRAILS / SOURCE ORE FIT REVIEW SAVED
