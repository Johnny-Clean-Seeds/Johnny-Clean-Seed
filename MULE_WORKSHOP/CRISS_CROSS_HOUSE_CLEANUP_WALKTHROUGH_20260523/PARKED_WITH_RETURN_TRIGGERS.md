# Parked With Return Triggers

Date: 2026-05-23

## P1. Home Path Sync

State:
PARKED / AUTHORITY-BOUNDARY

Why parked:
Old-home path appears in authority-adjacent surfaces, but editing `CURRENT_TRUTH_INDEX`, README, AGENTS, and teleporter pointers is path authority work.

Return trigger:
User selects Home Path Sync or asks to repair old home references.

Next proof:
Exact file list, backups/hashes, intended replacements, and no broad rewrite.

## P2. Root `COMMAND_CENTER` Pickup Lane Reconciliation

State:
PARKED / SOURCE-CUSTODY

Why parked:
Root `COMMAND_CENTER` contains the current pickup order and repair receipt, but active home has a separate `COMMAND_CENTER`.

Safe action already done:
Active-home custody pointer written.

Return trigger:
User selects pickup-lane reconciliation or asks to clean root `COMMAND_CENTER`.

Next proof:
Decide root porch pickup role: remain, move, copy, archive, or document as external pickup source.

## P3. Work Shed Reference Reconciliation

State:
PARKED / EXCLUDED-LANE REFERENCE REVIEW

Why parked:
Work Shed references remain, actual `HOUSE_WORK\WORK_SHED` is absent, and ignore rules exclude shed folders.

Return trigger:
User selects Work Shed reference reconciliation or a TODO cannot be resolved without old shed evidence.

Next proof:
Reference inventory and decision: historical, archive pointer, non-shed stub, or leave unchanged.

## P4. Local-Only Tracking Audit

State:
PARKED / GIT-LOCAL BOUNDARY

Why parked:
Tracked paths include local-only/private/shed-like names. No secret-like tracked paths were found by name scan, but labels and tracking state still disagree.

Return trigger:
User asks whether Git has all material, or selects local-only audit.

Next proof:
Per-path decision: keep tracked, untrack, relabel, split pointer/content, or block.

## P5. Root Tracked Support File Role Review

State:
PARKED / TRACKED FILE ROLE REVIEW

Why parked:
Root tracked helper/source files may belong in tool/source lanes, but moving tracked files is a repo reorganization.

Return trigger:
User selects root file role review or runner/support file cleanup.

Next proof:
Role table for each root support file, then safe move/keep decision.

## P6. Learning Lane Split Review

State:
PARKED / LANE SHAPE REVIEW

Why parked:
`BRAIN_LEARNING` is the main rule lane, but a `BRAIN\LEARNING` path also appears. This may be historical or intentional.

Return trigger:
User selects brain/rule lane cleanup or a search shows active rule material split across both.

Next proof:
Inventory both lanes and decide keep, pointer, merge candidate, or historical.

## P7. TODO / Parking Return Trigger Sweep

State:
PARKED / DEPENDS ON PATH CLARITY

Why parked:
TODO and parking have return triggers, but many TODOs reference Work Shed paths or old status layers.

Return trigger:
After Home Path Sync and Work Shed reference state are decided.

Next proof:
Proof-ranked TODO grouping with stale/current status and return triggers.


State:
PARKED / MULE REVIEW MATERIAL

Why parked:
The prior whole-home packet is useful but explicitly REVIEW / NOT PASS.

Return trigger:
User selects a row from `SET_ASIDE_NOT_THROW_AWAY_LIST.md` or asks for issue-review lane processing.

Next proof:
Selected row only, not the whole packet by inertia.
