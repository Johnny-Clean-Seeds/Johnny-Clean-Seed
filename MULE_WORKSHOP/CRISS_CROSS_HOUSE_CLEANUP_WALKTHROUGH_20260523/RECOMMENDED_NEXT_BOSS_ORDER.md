# Recommended Next Boss Order

Date: 2026-05-23

## Boss 1. Save / Lock This Criss-Cross Packet

Why:
The walkthrough packet and custody pointer are local changes. They are not committed or pushed.

Blocked:
Git/save boundary until user authorizes.

## Boss 2. Home Path Sync Candidate

Why:
Old-home references remain in authority-adjacent files and pointer files.

Scope:
Write a candidate first: file list, exact replacements, backups, hashes, no broad rewrite.

## Boss 3. Root `COMMAND_CENTER` Pickup Lane Reconciliation

Why:
Root pickup lane now has a work order and receipt while active-home pickup has only a pointer.

Scope:
Decide whether root `COMMAND_CENTER` is porch pickup, active lane candidate, or archive lane.

## Boss 4. Work Shed Reference Reconciliation

Why:
Work Shed references remain high while the active home has no actual Work Shed.

Scope:
Historical/archive/stub/leave decision only. No shed restoration by default.

## Boss 5. Local-Only Tracking Audit

Why:
Tracked paths still include local-only/private/shed-like naming.

Scope:
No untracking until per-path decision is reviewed.

## Boss 6. Root Tracked Support File Role Review

Why:
Tracked `.ps1`, `.md`, `.txt`, and `.url` support-shaped files sit at active-home root.

Scope:
Role table first, then keep/move candidate with hashes.

## Boss 7. TODO / Parking Return Trigger Sweep

Why:
TODOs and parked items have a good framework, but some references depend on path and Work Shed clarity.

Scope:
Run after Boss 2 and Boss 4, not before.


Why:
The prior mule whole-home packet has useful set-aside items, but it is REVIEW / NOT PASS.

Scope:
Select one item only when user opens that lane.

## Current Recommended Next Clean Action

If Git save is authorized, save this criss-cross packet first.

If continuing local repair without Git, start Boss 2 as a Home Path Sync candidate report only.
