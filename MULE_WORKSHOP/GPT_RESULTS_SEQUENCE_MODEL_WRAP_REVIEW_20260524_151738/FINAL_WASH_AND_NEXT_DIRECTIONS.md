# Final Wash And Next Directions

Status: FINAL WASH / SUPPORT ONLY
Date: 2026-05-24

## Washer Result

The clean result is:

`user tightening -> model activation -> output change -> rigidity watch -> controlled relaxation -> stage-weighted wrap`

The GPT sequence proves that a weaker/faster agent can improve when frame is applied repeatedly, but
the same pressure eventually creates rigidity. Clean Seed wraps should therefore train by phase, not
by one all-purpose mega-prompt.

## Adopted

- GPT_RESULTS saved as source custody.
- Sequence ledger saved.
- Model behavior finding saved.
- Wrap stage frame/freedom ratio saved.
- New support rule added: `WRAP_STAGE_AGENT_FRAME_FREEDOM_RATIO_RULE_20260524.md`.
- New support rule added: `SIGNAL_COMPLETION_CONTROLLED_RELAXATION_RULE_20260524.md`.
- End-of-job direction preference reinforced.

## Parked

| Item | Why parked | Return trigger |
|---|---|---|
| Exact numeric calibration | Risk of fake precision. | Repeated wrap tests need measurable comparison. |
| Separate prompts for every model type | Too early before first staged wrap trial. | User opens prompt-pack build for multiple bots. |
| Full rewrite of active guides | Current pass is support evidence only. | User opens active-guide integration pass. |
| Sandbox artifact recovery | GPT_RESULTS mentions sandbox downloads that are not local proof here. | User provides the actual files or requests artifact-source recovery. |

## Rejected As Active

- GPT output as doctrine.
- Tightening every agent with the same rigid frame.
- Calling a model trained because it explained the lesson.
- Letting "less intelligent" mean useless.
- Letting "more intelligent" mean no frame.

## Next Direction Options

1. Build the first staged Clean Seed wrap trial.
   - Start with Source Custody + Object Identity + Issue Replay Washer + Signal Stand-Down.
   - Best for proving the ratio on a real assistant workflow.

2. Build a bot-stage training matrix.
   - Rows: weak/fast, normal, strong/slow, rigid, drifting.
   - Columns: source intake, diagnosis, creative room, washer, closeout.
   - Best for planning many bot roles before implementation.

3. Run an active-guide integration review.
   - Compare the new support rule against active guides and decide what belongs upstream.
   - Best only after one real wrap trial proves the behavior.

4. Build a review closeout template.
   - Every review ends with summary, evidence, limits, next options, and best recommendation.
   - Best as a small helper, but not enough by itself.

5. Run a controlled-relaxation audit on parked material.
   - Find objects that say "might matter later" and convert them into "return when X" or source
     custody.
   - Best for reducing muscle tension, but narrower than the staged wrap trial.

## Best Next Move

Best next move:

`Build the first staged Clean Seed wrap trial.`

Why:

The central lesson is that training only counts when the next output changes and the signal can stand
down afterward. A staged wrap trial will prove whether the frame/freedom ratio and controlled
relaxation both work in the house, while a full guide rewrite would risk installing a beautiful
theory before it has survived live use.
